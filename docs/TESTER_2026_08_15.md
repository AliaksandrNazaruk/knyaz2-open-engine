# Отчёт тестера 15.08.2026 (Арыстан) — аудит по декомпиляту

Двадцать восемь пунктов. Правило прежнее и жёсткое: **канон — только ассемблер**
(engine/decompiled), наши доки, пак и клиент — подсудимые. У каждого пункта:
канон с адресами, что делает наш код сейчас (рабочее дерево, не git log),
вердикт, направление правки. Вердикты: ПОДТВЕРЖДЁН (расхождение с каноном) /
ПОЧИНЕНО (в рабочем дереве уже исправно) / НЕ БАГ (жалоба не по канону) /
ЖИВЬЁМ (нужен прогон на живом клиенте).

**Общий контекст, важный для многих вердиктов.** Последний коммит — 08.08;
правки по ряду жалоб лежат незакоммиченными в рабочей копии, пак
`content_build/` пересобирался сегодня, а снимок `content_build_canon_20260813/`
показывает ровно то, что видел тестер: он играл сборку от 13.08. Часть жалоб
уже закрыта в рабочем дереве — но до тестера это не доехало (по правилам
проекта выкладка только по просьбе). Перед повторным прогоном: коммит,
пересборка пака ТИТАНОВ (сейчас `content_build/manifest.json` — от
«Продолжения легенды», карты 153–249), выкладка с разрешения, сверка sha
отданных файлов (кэш Cloudflare).

---

## A. Квесты и лавки не помнятся (№3, №6, №28, №7)

**Канонная модель хранения (фундамент всех вердиктов).** Всё квестовое
состояние живёт в глобальных блоках, которые грузятся один раз за партию
(`0x43D898`: юниты 0x7B3C08, отряды 0x71E56C, поселения 0x83D408 — из GAME.n;
QUESTS.RES семью блоками, последний — состояния 300 диалогов 0x6A50E8) и
целиком пишутся в сейв (`0x423CB8:22-43`: 0x4B0 + 0x20000 + 0xC800 + 0x7D000 +
0x378C; сумма блоков сошлась побайтово с размерами QUESTS.RES 469 000 и
GAME.0 813 902). Читатель сейва `0x4236E0` сначала перечитывает всё с нуля,
потом накрывает блоками из сейва. Вход на карту НИ ОДИН блок не перечитывает.
Хранилища: (1) флаги игрока — байт +0xF9 (обработчик 34 ставит, 42 снимает,
условие 2 читает); (2) флаги собеседника — байт +0xF7 (микро-опы 0x04 в
`0x435F44:34-40`); (3) состояния диалогов 0x6A50E8, 300 × 4: бит 0x01
«разговор включён» (обработчики 62/61), бит 0x80 «квест взведён» (опы 0x05;
по нему же журнал `0x42A8F4:90` и MAP-патчи клеток `0x43DF48:94-107`);
(4) записи поселений. «Выключенных узлов» в движке НЕТ: машина узлов
`0x436478` пересчитывает условия при каждом открытии — неповторяемость
квеста держится только на изменённом состоянии.

**№3. Борье, юноша со сломанным мечом (Микула, диалог 94). ПОЧИНЕНО в
рабочем дереве + СЕЙВ-ДЫРА.**

Канон: закрывающая реплика несёт `h52(0), h46(0), h66(0)`; обработчик 46
(`0x433E30`) вычёркивает Микулу из отряда со сдвигом массива — юнит исчезает
навсегда, вход юнитов не перечитывает. Наш код: 46 ставит removed/hidden
(`dialog.js:1127-1145`), снимки жителей в `mapstate.js:206-235` снимаются на
обоих путях ухода, `units.js:413-414` removed не поднимает — починено
(непрокоммиченно). ОСТАТОЧНАЯ ДЫРА: сейв, снятый СТОЯ НА КАРТЕ, снимка
текущей карты не содержит — `mapStatePack` (`mapstate.js:276-289`) не зовёт
`mapStateCapture` (в отличие от `villagePack`), а `packFoe`/`applyFoes`
(`save.js:130-199`) не несут removed/hidden/flags/bag — после загрузки
Микула вернётся с квестом. В движке дыры нет: сейв пишет массив юнитов
целиком. СДЕЛАНО 15.08.2026: `saveState` первым делом зовёт
`mapStateCapture(mapNumber, units, loot)` — как `villagePack` зовёт
`villageCapture`; расширять packFoe/applyFoes не понадобилось (снимок
жителей в памяти карты покрывает removed/flags/bag, а бут разворачивает
её до расстановки). Сейв v4. Два новых теста в SaveCoverageContractTest:
снимок обязан идти ДО упаковки; мировой такт — сохраняться и
восстанавливаться до сборки карты. pytest: 640 passed, 2315 subtests.

**№6. Борье, покупка дома (Никифор, диалог 73). ПОЧИНЕНО + та же сейв-дыра.**

Канон: гейты — бит собеседника (+0xF7), quest39 (бит 0x80) и уход Никифора
(46); повторную покупку в оригинале исключает именно исчезновение юнита.
Наш код: биты собеседника с устойчивым ключом «карта:слот:бит»
(`dialog.js:267-275`) и `dialog.quests` — в сейве (`save.js:316,322`);
уход — как №3. Вердикт и правка — как №3.

**№28. Нижний лагерь: все квесты сбросились. На сборке тестера — ожидаемо;
по текущему коду ЖИВЬЁМ.**

Карта 37 — поселение (запись №3 в GAME.0). Сбросов dialog.quests/flags при
переходах в текущем коде нет (единственный `questsReset` — старт партии,
`app.js:963`); ключи памяти карт сверены с манифестом — расхождений 0.
Без непрокоммиченных mapstate/village/shops жители и биты пересоздавались из
пака при каждом входе — жалоба описывает сборку 13.08. Частично
воспроизводится и сейчас через «сейв на карте → загрузка» (дыра №3).
Проверить живьём на свежей сборке.

**№7. Волхв у Борье (карта 17): ассортимент заново. ПОЧИНЕНО + та же
сейв-дыра. Канон неожиданный: у карты 17 ЗАПИСИ ПОСЕЛЕНИЯ НЕТ.**

Массив поселений прочитан прямо из GAME.0 (смещение 799 522): 12 записей —
карты 19, 33, 13, 37, 21, 20, 18, 23, 25, 39 + две пустые. Искатель
`0x43BF08` возвращает NULL; вход (`0x43DF48:209-232`) генератор прилавка на
карте 17 НЕ зовёт; торговля (обработчик 38, `0x43346C:38-76`) при отсутствии
ролей поселения торгует ЛИЧНЫМ МЕШКОМ собеседника (+0x62, 42 слота; деньги
+0x26); сделка переписывает мешок юнита (`0x41F638:217-247`). Мешок Светогора
(6 предметов из GAME.n) конечен навсегда — «ассортимент заново» в оригинале
невозможен в принципе. Наш код: роль 0 → торговля мешком (`dialog.js:664-678`),
`shopsRestock` роль 0 не трогает, мешок/деньги жителя в снимке
(`mapstate.js:138-139`, `units.js:461-462`) — починено. Через «сейв на
карте → загрузка» мешок и сейчас откатится к паку (packFoe без bag/money).

**Смежные находки по канону (вне жалоб).**

1. Выход на глобальную не зовёт `villageCapture()` (`app.js:1066-1071`;
   переход между локациями зовёт — `app.js:619`): поля
   incomeStamp/trainTimer/lastTime откатываются.
2. Хвост прилавка купца 14–31 ПЕРЕВЫПЕКАЕТСЯ каждый вход: канон
   `0x41896C:181+` чистит и заново наполняет места 0xE–0x1F (18 гарантированных
   товаров); наш `restockGoods` (`shops.js:167-213`) знает только места 0–13
   с гейтом «пустое» — у купцов деревень ассортимент систематически беднее
   канона (это же — старая жалоба D3 «скудный ассортимент»). Формула «только
   пустые» верна лишь для мест 0–13.
3. Банки знахаря — в места 0 и 13 (шаг 0x1A байт), не 0–1. Косметика.
4. `mapStatePack` не снимает текущую карту — корень сейв-дыры.
5. Рабочий `content_build` не содержит карт Титанов (манифест 153–249) —
   прогоны жалоб только на `content_build_canon_20260813` или после полной
   пересборки.

---

## B. Кучи, тайники, земля (№2, №14, №27)

**№2. Беглое: топор на земле не показывается. ПОДТВЕРЖДЁН (клиент).**

Канон. Кучи не читаются из файла карты: при новой игре `0x43D898:31` читает из
GAME.x архив куч `FUN_00442fdc(f, &DAT_00794d5c, 0x18a88)` — смещение файла
0x2C800, 1000 записей × 0x65; счётчик 0x8496D0 — до первого нулевого байта
карты (+0x08). Архив живёт весь сеанс; вход на карту (`0x43DF48:313-358`)
строит из него живую таблицу 0x7AD7E4 (лимит 200, счётчик 0x8496A8): напольной
(+0x09 == 0xFF) ставится бит клетки 0x2000, гнездовая привязывается к гнезду
зоны (таблица 0x6AE45C). Спрайт выбирает `0x43BBC4`: одна вещь и нет денег →
собственная картинка класса вещи (у топора спрайт 154), иначе мешочек 0xA3.
Рисуют проход земли `0x424FD8:206-243` и проход нутра построек `0x424514:137+`.
Сам топор сверен с сырым GAME.0: запись 336, карта 18 (Беглое), клетка 54:47,
зона 0xFF, одна вещь класса 138 «Обоюдоострый топор»; все 10 записей карты 18
сходятся с паком байт в байт.

Наш код. Пак довозит всё (`content_build/maps/18/map.json`, pile_336,
`items['class:138'].ground = assets/icons/ui_154.png`). Ломается загрузка
картинки: `loot.js:270-277` `drawPile` при отсутствии образа молча выходит и
не заказывает его; спрайты земли грузит только стартовый путь
(`app.js:1146` `lootAssets()`), а путь перехода `enterMapInner`
(`app.js:719-722`) их в preload не включает, и `queueItemIcons`
(`app.js:559-563`) шлёт `item.icon`, но не `item.ground`. Итог: на карте,
куда вошли переходом, одиночные напольные кучи (топор ui_154, Белый корень
ui_168, Земляной орех ui_160 — все кучи Беглого) не рисуются; после загрузки
сейвом прямо на карте — рисуются.

СДЕЛАНО 15.08.2026: `lootAssets()` в предзагрузке `enterMapInner` (lootSetup
уже отработал — список от новой карты) + `drawPile` при промахе заказывает
спрайт через `world.requestAsset` и пропускает кадр.

**№14. Пустой тайник становится недоступным. ПОДТВЕРЖДЁН (частично; для
напольных куч тестер неправ).**

Канон. Закрытие обмена с кучей `0x41F638:131-163`: слоты кучи переписываются
из окна (класть своё МОЖНО), и лишь если куча пуста — `FUN_004136a8(куча)`.
Всё тело удаления `0x4136A8` стоит под `if (байт +0x09 == 0xFF)`: удаляется
только НАПОЛЬНАЯ куча (бит клетки снят `&= 0xDF`, живая таблица уплотнена,
архивная запись освобождена). Опустевший сундук/бочка (гнездо обстановки)
не удаляется никогда — пустой контейнер открывается и принимает вещи.
Грядка 'Q'/'R' вместо удаления получает метку отрастания `-1 - такт/360`.
Раскопанный схрон: `0x4115AC:63-65` — деньги игроку и слово +0x0F (знак
«закопана») обнуляется: после первого обыска с лопатой схрон — обычная
видимая куча. Положить на землю `0x423360`: в существующую кучу (до 0x2A
вещей) или новой записью — на месте бывшего тайника класть можно всегда.

Наш код. Три расхождения: (1) `trade.js:356-357` ставит `taken = true` без
проверки гнезда — опустевший сундук умирает для курсора
(`furniture.js:63`), и через сейв `taken` переживает загрузку
(`save.js:315,595-602`); (2) правильный гейт уже написан в `loot.js:249-267`
`lootTake` (гнездо/грядка/напольная) — но это мёртвый код, вызовов нет;
(3) `combat.js:1026-1053` `openPile` не снимает `buried` — раскопанный, но не
опустошённый схрон навсегда невидим и требует лопату при каждом обыске,
против `0x4115AC:65`. Попутно: `tradeFinish` не помечает грядку — трава,
сорванная через окно обмена, не отрастает (канонная ветка `0x4136A8:31-45`
и проверка на входе `0x43DF48:316-317`, порог ровно 0x1517+1 = 5760 тактов).

СДЕЛАНО 15.08.2026: общая `lootEmptied(pile, held)` в loot.js — гнездовая
остаётся, грядка получает метку, напольная уходит; зовут её и `lootTake`,
и `tradeFinish` (прежний безусловный `taken` убран); `openPile` снимает
знак закопанной (`dug`) безусловно, как `0x4115AC:65`. Уточнение канона
чтением 0x4136A8 целиком: строка 21 проверяет не деньги, а ВТОРОЕ слово
предметов (+0x13) — грядкой становится куча, державшая РОВНО ОДНУ вещь
класса 'Q'/'R' (деньги гейтит вызывающий 0x41F638); решает содержимое, а
не происхождение — посадка растений игроком (выброшенный орех) работает
той же веткой. 4 новых контрактных теста (PileEmptiedContractTest).

**№27. Чёрный Бор: выброс на глобальную при взятии доспеха, доспех вечный.
ПОЧИНЕНО в рабочем дереве + ЖИВЬЁМ.**

Канон. Дверь зовётся из единственного места — разбора прибытия `0x4115AC`:
switch по занятию юнита; case 3 (обыск) открывает обмен и до двери НЕ доходит,
дверь только в default (бит 0x1000 клетки → `FUN_00420900`). Пришедший
обыскать телепортироваться не может по построению switch. Персистентность:
архив куч живёт сеанс, уход с карты пишет живых обратно (`0x43A628:107-142`),
сейв возит обе таблицы (`0x423CB8:36-38`); файл карты при повторном входе для
куч не читается — взятое не возрождается никогда.

Наш код. Гейт уже стоит: `exits.js:132-160` — дверь только при
`!busyKinds.has(hero.orderKind)` (talk 2, take 3, wait_talk 0x0C); приказ
«взять» действительно ставит kind 3 (`combat.js:757,768-770`); кромка
«шёл→встал» в `exitsTick` ловится в тик, когда `orderKind` ещё 3. Память куч
снимается на обоих путях ухода (`app.js:612,1067` → `mapStateCapture`),
`lootSetup` предпочитает память паку, сейв возит и кучи, и память карт.
Возрождение доспеха было следствием телепорта (снимок делался, пока доспех
лежал). Гейт держится на тонком порядке тиков — прогнать живьём на самой
куче Чёрного Бора.

Правка (укрепление, не срочно): в `exitsTick` проверять вид приказа, с
которым юнит ПРИБЫЛ, а не `orderKind` текущего тика.

**Попутные находки.**

1. `content_build/manifest.json` сейчас от «Продолжения легенды» (карты
   153–249): карты Титанов лежат в `content_build/maps/18/...`, но в манифесте
   не числятся — локальный клиент скажет «карты 18 нет в паке». Перед любой
   проверкой живьём пересобрать пак Титанов (грабля «Договор пака и клиента»).
2. Мёртвый `lootTake` и неотрастающие после окна обмена грядки — см. №14.

---

## C. Клоны, должности, спутники (№13, №18, №24, №12, №21, №16)

**Поправка номеров обработчиков** (таблица 0x462E90 снята с konung2.exe,
DGROUP offset 0x4EA90, 76 указателей): 36→0x433070 «взять в отряд»,
43→0x4338B0 «сделать жителем», 46→0x433E30 «удалить из мира»,
74→0x435D4C «назначить на должность», 29→0x435500/30→0x435550 «должность
занята / занимает ли собеседник». НЕ подтвердилось из прежних записей:
39/40/41 — статус деревни (+0x49D), перезакладка цен и залог, НЕ должности;
71/72 — казна/флаги деревни, не найм. Обработчик 43 в клиенте уже есть
(память проекта устарела); обработчик 25 (0x435390, условие по текущей
карте) — отсутствует и сейчас.

**Сквозной корень всех пяти жалоб:** в каноне юнит — одна запись в одном
глобальном массиве 0x7B3C08 (свёртка карты 0x43A628:37 вырезает только
мёртвых `(local_34[0x1a] & 0x80) != 0`; вход 0x43DF48 массив юнитов не
трогает). В порте идентичность юнита живёт в ТРЁХ незакрытых представлениях:
пачный `unit_N`, спутник `mate_N`, запись памяти по слоту.

**№13 + №18. Два Белуна, клон ходит следом. ПОДТВЕРЖДЁН.**

Белун — unit_346 карты 18, родная деревня Борье (33): отпуск — узел 1345
«…хочу на могилу родителей сходить», повторный наём — узел 3 (гейт F1) →
обработчик 36. Наша цепочка дубля доказана построчно: отпуск
(`dialog.js:1231`) пишет запись приёмыша `mapStateJoin(33, unit, member)`;
повторный наём (`dialog.js:795` → `units.js:246 partyHire`) запись НЕ удаляет;
при уходе `mapstate.js:102-112 refreshJoined` освежает её от живого
юнита-союзника (ally, приказ следования 0x10/0x30); при входе хвостовой цикл
`units.js:426-432` поднимает её вторым экземпляром без проверки inParty
(Белуна в паке Борья нет, пачный путь запись не потребляет). Итог: спутник в
отряде + клон из памяти с ally и битом следования — «ходит за мной».
Сцена могилы юнитов не трогает — нужен лишь цикл «отпустил → нанял →
сходил на соседнюю карту → вернулся».

СДЕЛАНО 15.08.2026 тройной защитой: (1) наём (36) снимает запись приёмыша —
`mapStateUnjoin` в mapstate.js; (2) хвостовой цикл unitsSetup пропускает
записи, чей ключ «родная карта:слот» уже в отряде; (3) refreshJoined не
освежает запись от союзника (слот не глобален — под тем же номером может
стоять спутник чужой карты).

**№24. Ярл не даёт квест, «превращается в Белуна» (Нижний лагерь, 37).
НАПОЛОВИНУ ПОЧИНЕНО (непрокоммиченная правка) + ЖИВЬЁМ.**

Канон: слот уникален глобально; «Ярл» — Адльстайн Рыжий, unit_277, диалог 23;
пересечений слотов с Белуном (346) нет; 0x432D1C ищет юнита по номеру в
глобальном массиве с любой карты. Наш механизм «запись отряда прячет пачного
юнита любой карты с тем же слотом» признан в `units.js:386-392` и закрыт
правкой (ключ `${member.home ?? mapNumber}:${member.index}`). Щели:
(1) записи БЕЗ home прячут по-старому — а home=None у старых сейвов и у
стартовой пары в shared.json (Путята index=1, Тур index=2); на свежей игре
не стреляет (unit_1/unit_2 нет ни на одной карте мира 0); (2) клон-механизм
№13 работает и в Нижнем лагере (деревня, «останься здесь» доступен);
(3) чистка «подойди и заговори» (61) ищет цель только на текущей карте
(`dialog.js:1414-1418`), движок — в глобальном массиве.

СДЕЛАНО 15.08.2026 (кроме мелкой щели): выпечка кладёт бойцам `home` — карту
самого отряда (u16 +0x08 записи, `PARTY_MAP_AT`; движок берёт стартовую
карту ровно оттуда, VA 0x438A00); бут мигрирует старые сейвы — home
проставляется членам, совпавшим по index с пачным шаблоном (стартовая
пара), нанятых в старом сейве не трогаем (их родная карта неизвестна,
прежнее поведение безопаснее неверного ключа). Клон-механизм №13 закрыт.
ТРЕБУЕТ ПЕРЕСБОРКИ ПАКА (shared.json). Остаётся щель: чистка «подойди и
заговори» (61) ищет цель только на текущей карте — движок ищет в
глобальном массиве; мелко, отдельным пунктом.

**№12. Не назначить Белуна воеводой (Борье). ПОДТВЕРЖДЁН (два корня).**

Канон: 0x435D4C (74): место свободно (`слово +0x3D0 == 0`) → `FUN_004338b0(0)`
с отказом при полном отряде деревни (счётчик +3 против предела +1) → запись
слота в должность. Реплика: условие `H28(1) AND H4(4) AND NOT H29(4)` —
нужна ПОСТРОЕННАЯ казарма (слот 4, kind 13; скрытая реплика без казармы —
канон, не баг). Обработчик 36 (0x433070) при заборе жителя обнуляет его
должность и убавляет счётчик деревни.

Наш код: `dialog.js:1332-1368` порядок повторяет, но (1) наш 36
(`dialog.js:780-798`) officials НЕ трогает — Путята держит место 4 навсегда;
(2) `squad_people` только растёт (`dialog.js:1213` — единственная запись,
декремента нет нигде): Борье стартует 14/17 — после трёх циклов
«отпустил/забрал» деревня «полна», и 43, а с ним 74, молча возвращают false.

СДЕЛАНО 15.08.2026: обработчик 36 освобождает должность юнита (indexOf по
слоту → 0) и убавляет squad_people, если юнит был в отряде этого поселения
(сторона сверяется ДО переписи на сторону игрока); обработчик 46 убавляет
счётчик в иначе-ветке (житель своей деревни), как 0x433E30:101-109.

**№21. Путята-воевода: «не могу забрать». ПОДТВЕРЖДЁН.**

Канон: условие 30 (0x435550) сравнивает слово должности с индексом
СОБЕСЕДНИКА в глобальном массиве. Корень Путяты (узел 5169): br1 `H30(4)` →
«Какие будут распоряжения?» → «Мне нужны опытные воины» (H36, гейт H7).

Наш код: `dialog.js:653-657` — H30 берёт номер из строки id:
`Number("mate_1".replace("unit_",""))` = NaN (экс-спутник всегда `mate_N`,
`units.js:270`, а должность записана слотом, `dialog.js:1342`). NaN ≠ 1 —
воеводская ветка не срабатывает никогда, разговор падает в br6; и даже
забрав через гейт H7 — должность остаётся занятой (№12). Следствие-бонус:
обучение в казарме (`village.js:357-358`) ищет воеводу по слоту среди живых
и найдёт спутника, гуляющего в отряде.

СДЕЛАНО 15.08.2026: условие 30 сравнивает `officials[argument]` со слотом
собеседника (id больше не парсится); вместе с правками 36 из №12 Путята
отдаётся и место освобождается.

**№16. Лучеслав: иконка в отряде, стоит рядом, квест повторяется.
ПОДТВЕРЖДЁН.**

Канон: отпуск Лучеслава — НЕ 43, а 46: узел 4582 «Я узнаю это место!..»
opt0 → `H52(0), H46(0), H66(0)`. 0x433E30 (46), ветка «сторона игрока»
(строка 29): счёт отряда −1 + сдвиг хвоста — юнит вычеркнут из отряда;
повторение исключено самим удалением записи.

Наш код: наш 46 (`dialog.js:1127-1146`) ставит removed/hidden и чистит
officials, но НЕ вычёркивает из hero.party.members. Панель рисуется из
members (`ui.js:988`) — «иконка остаётся»; partyCapture (`units.js:229-241`)
запись сохраняет (removed не смотрит); на входе spawnCompanion
(`units.js:372-375`) ставит его рядом с героем — «снова стоит рядом»;
корень 4578 br0 `H32(0)` снова ведёт к 4582 — квест повторяется.

СДЕЛАНО 15.08.2026: обработчик 46 для союзника повторяет ветку «сторона
игрока» 0x433E30:29-35 — partyRelease + deselect + снятие ally (без этого
снимок жителей, который берёт только не-союзников, не увидел бы removed).
Запись из party.members уходит, partyCapture возвращать нечего.

---

## D. Предметы и дружина (№4, №15, №11, №9)

**№4. Вещь пропадает при переносе на портрет дружинника. ПОЧИНЕНО
(непрокоммиченно, после жалобы) + ЖИВЬЁМ.**

Канон: в движке потерять несомую вещь физически невозможно. Бросок на
портрет (`0x421690:292-307`): портрет мёртвого → попытка применить зелье
(`0x41F55C`); не владелец вещи → `FUN_00423218(цель, -1)` — в мешок ЦЕЛИ;
свой портрет → применить на себе. `0x423218` — ровно три отказа, все с
возвратом вещи владельцу через `0x42944C(1)`: квестовая (цена 0) чужому;
перегруз `Вын×1000/3 + 20000 < вес` («Превышен вес»); все 42 ячейки заняты
(«Мешок у тебя не резиновый!»). Плюс страховка WndProc (`0x42F22C:176-178`):
вещь, оставшаяся «в руке» после щелчка, принудительно возвращается владельцу.

Наш код: `ui.js:1013-1023` + `carry.js:769-813` — ветки квестовой/веса/
полного мешка с возвратом, экземплярные поля (чары/отрава/счёт пачки) теперь
переезжают получателю. В HEAD портрет только выбирал юнита, а экземплярные
поля оставались у отдающего — стопка без счёта невидима, ровно симптом
тестера. Прогнать живьём: обычная вещь, пачка стрел, зачарованная,
квестовая, полный мешок. Два канонных хвоста: бросок на портрет МЁРТВОГО
должен пытаться применить (живая вода), а «свой/чужой» сравнивать по
владельцу вещи (`carry.owner`), не по `carryActor()`.

**№15. Дружинники не могут применять свитки/лапки/глаз. ПОДТВЕРЖДЁН.**

Канон: правая кнопка (`0x422AFC:19-44`) берёт вещь из мешка юнита, ЧЬЯ
ПАНЕЛЬ ОТКРЫТА (0x849514), и применяет вид 9 (выпить) / вид 11 (применить)
НА ЭТОГО юнита; то же при броске на портрет (`0x41F55C:11-27`). Внутри
`0x436C48` всё действует на применяющего: класс 42 «Соколиная лапка» →
+3 к характеристике 4 юнита (`0x436BA8`, кламп 0..150); класс 39 «Свиток
ведуна» → опознать всё у юнита (`0x41B7C0`); классы 44/45/48/50 → +10 к
навыку; класс 49 → опыт; съеденное удаляется из мешка применяющего.
Герой-центричны только береста/уголёк/ястреб (случаи 0/0x15/0x16 — там
явно 0x84951C).

Наш код: `carry.js:538` `if (actor === hero && questItemUsable(name))` —
для спутника ветка мертва (комментарий ссылается на 0x434F8C, но это
диалоговое условие, не путь правой кнопки); `questitems.js` целиком на
героя (`hero.bag`, `hero.skills`). Зелья у спутников уже работают
(`carry.js:432-434`). Дополнительно: класс 39 «Свиток ведуна» (опознать
всё) не реализован вообще ни для кого.

Правка: снять гвоздь `actor === hero`, пробросить `actor` сквозь
questitems.js (искать/тратить/применять у применяющего); героевыми оставить
только 0/21/22; добавить класс 39 → identifyAll(actor) (`jewels.js:97+`
уже умеет).

**№11. Топоры не надеваются при выносливости 55. НЕ ВОСПРОИЗВОДИТСЯ по коду
(вероятно, тестер про Секиру/Палицу — они и в оригинале не дадутся) + ЖИВЬЁМ.**

Канон: проверка `0x418648`: (1) неопознанная вещь не надевается вовсе;
(2) гейт по КЛАССУ (таблица 0x45D424: класс 0 → Вын 50, класс 1 → Вын 30,
классы 8..15 → Ловкость 100); (3) гейт по ВИДУ (таблица 0x45DAF0, шорты
+0x0C стат / +0x0E порог). Характеристика — ТЕКУЩАЯ (+0xCC..+0xD1).
Таблица видов из exe: Меч 36 Сила, Добрый меч 65 Сила; Топор —
ВЫНОСЛИВОСТЬ 36, Двуручный топор — Вын 26, Дубина — Вын 10; но Секира —
Вын 83, Палица — Вын 65, Двуручная секира — Вын 83; луки — Ловкость.

Наш код: `inventory.js:183-198` — формула, стата и числа совпадают с exe
(порядок имён сверен по shared.json, пак читает те же поля 0x45DAF0). Топор
с порогом 36 при Вын 55 обязан надеваться. Объяснения жалобы в рамках
канона: «топоры» тестера — Секира (83), Палица (65), Обоюдоострый топор
(55 — впритык), либо вещь не опознана (бит 0x8000 слова чар — отказ с тем
же сообщением «Предмет не доступен для использования»). Живьём: снять класс
и слово чар конкретного экземпляра (agent.js snapshot).

Правка: показать порог в подсказке вещи («Выносливость 83») — снимет
ложные жалобы. Найденное расхождение в ДРУГУЮ сторону: второй гейт по
КЛАССУ (0x45D424) у нас отсутствует — порт мягче канона; в реестр.

**№9. Охотник забирает бутылёк целиком. НЕ БАГ — канон подтверждает наш код.**

Канон: диалог охотника (узлы 3546-3559, вход по условию 17(88) «предмет в
мешке»), развязка 3558 — действия [45(88), quest, 51(100)]. Обработчик 45
«забрать предмет» (`0x433D38:10-27`): первая вещь вида 88 (Противоядие) в
мешке героя, запись освобождается ЦЕЛИКОМ (`= 0xFF`), мешок сжимается.
Никакого «отпить долю» в квесте нет; частичный расход объёма существует
только при ПИТЬЕ зелья самим юнитом (`0x41D954`: противоядие убывает
пропорционально отравлению, пустая банка — вид 0x53). Начатый бутылёк
сохраняет вид 88 и забирается так же — «любой бутылёк целиком» и есть
оригинальное поведение. Наш `dialog.js:587-597` — дословный перенос.
Тестер помнит механику питья, не квеста. «Бережность» (брать слабейший)
была бы отклонением — только осознанно и с записью в реестр.

**Попутно:** приказа «дать вещь» в движке не существует — передача мгновенна
через мешок цели, идти к цели не нужно; сообщения отказов в паке уже
канонные (сверены со строками exe 0x4504E2/0x4504EF/0x45024C).

---

## E. Бой, засады, благословение (№26, №20, №1)

**№26. Сила удара падает вместе со здоровьем. ПОЧИНЕНО (непрокоммиченная
правка; тестер играл сборку без неё).**

Канон. Функция силы удара `0x41A7D0`; FPU в декомпиляте съеден Гидрой,
формула поднята дизассемблером из exe (все три ветки):
тварь — `TRUNC(Сила(+0xD0) × 0.04 × hp(+0x4E) × 0.0625)`; человек с оружием —
урон класса `[0x45DAF4 + класс×0x20]` ПЛЮС тот же член (0.04/0.0625); голая
рука — 0.02/0.0625, вдвое слабее; затем чары +0x42, кламп ≥ 0. Урон
масштабируется ЛИНЕЙНО от ТЕКУЩЕГО здоровья бьющего (hp 0..1600 → 0..100;
при полном здоровье прибавка = Сила×4 с оружием). Поля «усталость» в записи
юнита нет вовсе. Резолвер (дизасм 0x41C244–0x41C2E1):
`сырое = max(0, сила×0.7 − броня) + сила×0.3;
урон = TRUNC((сырое / стойкость(+0xD1) + 1) × 16)`.

Наш код. `combat.js:367-377` `mightTerm()` — точное повторение (TRUNC,
scale 0.04/0.02, hp в сырых 0..1600), применён во всех ветках `strengthOf`;
у выстрела члена нет (как 0x41A52C). В `git show HEAD:combat.js` правки нет —
она свежая, непрокоммиченная. Прогнать pytest, закоммитить, довезти.

**№20. Предупреждающий экран при нападении в пути. ПОДТВЕРЖДЁН (и в HEAD,
и в рабочем дереве).**

Канон. Такт глобальной `0x4277F4`: столкновение с бродячим отрядом
(строки 192-254; каждый 16-й кадр, клетка отряда == клетке игрока,
`rand()%100 >` Следопыт (+0xDF)) → открывается картинка из PICS\ENCOUNTS.RES
на весь экран, `_DAT_008495E8 = 1`; случайная встреча местности (строки
163-189) — то же, морская сцена 0x1B берёт SBUTTLE.RES. Главный цикл
`0x438A00:207` НЕ грузит карту боя, пока флаг = 1; загрузчик `0x43DF48:388-392`
при взведённом флаге ставит режим 10 и рисует спрайт INTERF.RES №230
(495×47 — лента «нажмите любую клавишу») в (517, 710). WndProc `0x42F22C`:
WM_KEYDOWN в режиме 10 → `FUN_0043A5CC(1)`, WM_LBUTTONUP → `FUN_0043A5CC(0)` —
сброс и пересборка экрана, игрок в бою. Тот же механизм у входа в любую
локацию (`0x422CCC`: M<номер>.RES / SEA.RES / RANDOM.RES).

Наш код. `app.js:356-381` `meetEnemy()`: строка «Засада!» и сразу
`enterMap(met.scene)` — ни картинки, ни ожидания.

Правка: испечь `PICS/encounts.res` и `PICS/sbuttle.res` в пак (формат разобран
в `konung2/pics.py` — вот зачем он хранился), показывать картинку поверх,
карту грузить под ней, снимать по любой клавише/клику. Это же каркас для
отложенных родных экранов загрузки локаций (M##.RES).

**№1. Благословление волхва. ПОДТВЕРЖДЁН ПО СУТИ — старый вывод E2
(«эффекта нет») ОПРОВЕРГНУТ. Канон: +15 Харизмы и +10 Интеллекта, пока флаг
взведён (тестер неточен в числе).**

Канон. Диалог перепроверен по сырому QUESTS.RES: узел 4833 несёт ровно два
действия — обработчик 34 арг 1 (`0x3000122`) и 60 арг −40 (`0x3FFD83C`);
обработчика 53 «поднять харизму» нет — прошлые данные верны. Но эффект живёт
не в разговоре, а в движке: обработчик 34 (`0x432ED0`) делает
`игрок[+0xF9] |= арг` (квестовые флаги — байт +0xF9 записи вожака);
пересчёт характеристик `0x41C494:72-74` (перед каждым расчётом и после
каждого урона): `if (юнит[+0xF9] != 0) FUN_0041844C(+0xF9)` — флаговый байт
скармливается разборщику прибавок наравне с «номером прибавки» предмета.
`0x41844C`: значения 1..15 — битовая маска строк 8/4/2/1 таблицы прибавок
0x45D431 (шаг 16, до трёх троек «стат, режим, величина»). Строка 1 (бит 1 =
благословение), прочитано из exe по абсолютным адресам:
`[0x45D444]=1,[0x45D446]=+15; [0x45D448]=3,[0x45D44A]=+10` → **Харизма +15,
Интеллект +10**. Бонус ложится на эффективные статы +0xCC/+0xCE при каждом
пересчёте — действует, пока флаг стоит, виден в панели и в диалоговом
условии 9 (0x434DB0 читает +0xCC). Соседние биты: 2 — проклятие (Ловкость
−50, броня −100, точность −200; не ставится, пока в мешке предмет класса 33),
4 — здоровье +80×16, 8 — Сила +300; бит 1 взводится и предметом класса 41
(`0x436C48:238`).

Наш код. Обработчики 34/42/47 и условие 2 в `dialog.js:851,896-909` есть,
но `hero.flags` дальше НИГДЕ не используется: `jewels.js:57-65
currentCharacteristics` складывает только зачарования надетого; таблицы
0x45D431 в паке нет. Эффекта на статы нет.

Правка: испечь строки 0x45D431 в пак (стат/режим/величина; статы 7 = броня
→ +0x42, 8 = точность → +0x44, 10 = здоровье×16) и применять
`hero.flags`/`unit.flags` как маску строк при пересчёте статов (jewels.js +
боевые statsOf), режим «заменить/сложить» по байту режима. Механизм общий
для флагов и «номера прибавки» предмета (+1 записи, 0x6F956D); флаг действует
у любого юнита, не только героя.

---

## G. Деревня: тренировка, стройка, трава (№5, №25, №17)

**№5. В казарме никто не тренируется. ЧАСТИЧНО ПОЧИНЕНО / ЧАСТИЧНО
ПОДТВЕРЖДЁН (тренировка есть, но НЕВИДИМА).**

Канон, три части. Пассивная учёба `0x4181E8` (из деревенского тика
`0x41C944:513-517`, только в такт без старта фаз стройки): гейт — воевода в
слоте +0x3D8; период 1200 деревенских тактов (≈25 мин); ученик уровня ≤
уровня воеводы получает +100 опыта, +25 очков, дважды авто-рост статов
(5,4,1) и 11 навыков; тикает для ВСЕХ 12 поселений независимо от текущей
карты. Визуальная: выбор точки досуга `0x412C0C` — точки вида ≥0x70 доступны,
если у группы снят бит 0x80 в +0x1E ИЛИ назначен воевода; день/ночь по биту
0x10; срок rand(180)+60. Приход (`0x4115AC` case 4): вид 0x90/0xA0 → занятие
9 (спарринг), 0x70/0x80 → занятие 10 (чучело). Тик занятий `0x413894`:
таймер +0xF5 раз в 16 тактов; занятие КРУТИТ БОЕВУЮ АНИМАЦИЮ (для 10 фаза
случайная `rand(кадры/2)`; для 9 — партнёр в 2 клетках, разворот навстречу,
+1 опыта партнёру раз в 1024 такта); направление — нижний ниббл вида точки.
VILLAGE_TRAINING_SPEC в основном подтверждён, открытые вопросы закрыты.

Наш код: пассивная учёба (`village.js:342-381`) и каркас точек/приказов
9/10/опыта (`units.js:880-921,1646-1685,862-877`) есть и канонны; пак Беглого
несёт казарму, воеводу и точки. Но тестер тренировку не увидит и сейчас:
приказы 9/10 НЕ ИМЕЮТ АНИМАЦИИ (обработчика приказа 10 в units.js нет
вовсе) — юнит стоит в позе stand; нет разворота по нибблу; гейт упрощён до
«всегда нужен воевода» (в неуправляемых деревнях канон тренирует и без
него); учёба не тикает вне текущей карты.

Правка: в такте занятий 9/10 ставить позу удара с канонной случайной фазой и
разворотом; оффмап-тик учёбы — вместе с №25.

**№25. Стройка кузницы в Беглом встала. ПОДТВЕРЖДЁН (клиент, два корня).**

Канон: весь цикл — `0x41C944`, ветка `:356` раз в 16 мировых тактов для ВСЕХ
12 поселений массива 0x83D408 независимо от текущей карты. Работники:
юниты отряда поселения (облик +0xFC < 6, не трупы, порода не 3/0xB/0xC)
минус должностные (5 слов +0x3D0..+0x3D8); при тревоге отряда — ноль.
Решающее (строки 455-505): при ступени <3 И работниках >0 счётчик убывает и
на нуле `счётчик = (время_вида × 60) / работники`; ИНАЧЕ (нет работников или
ступень ≥3) счётчик ВСЁ РАВНО тикает по burn_step (`0x45D84C + вид×0x10`) —
**канонная стройка не останавливается никогда**: недострой без работников
конвейером догорает 3 → 4 → 5 → 6 (пепелище), пепелище при работниках
расчищается в 0. Заказ `0x434BC4`+`0x433730`: сырой срок без деления, гейт —
лучший навык 18 отряда ≥ порога вида.

Наш код: формулы и переходы канонны (`buildings.js:98-158`,
`dialog.js:1157-1181,717-731`), НО (1) `effects.js:104-116` тикает только
объекты ТЕКУЩЕЙ карты — чужие поселения из склада не тикают ничем: ушёл с
карты — стройка стоит (главный корень жалобы); (2) `buildings.js:118-119`
паузит счётчик при 0 работников (ночь, бой, малый отряд) — канон продолжает
по ветке burn_step. Вечного стопа при живых работниках на загруженной карте
не воспроизводится (кузница ≈ 8 мин при 8 работниках).

Правка: тикать стройку по записям мест всех запомненных поселений (склад
settlements), объект карты лишь отображать по месту (место ведущее,
`0x43DF48:222-231`); при 0 работников вести ветку B — включая канонное
догорание недостроя (если этот канон жесток — отклоняться осознанно, с
записью в docs).

**№17. Трава не респавнится. ПОДТВЕРЖДЁН (клиент, два корня).**

Канон: трава — не объект карты, а куча-грядка (архив 0x794D5C; вид записи 9,
класс 'Q' 0x51 / 'R' 0x52, без денег). При опустошении `0x4136A8` вместо
освобождения пишет метку `−1 − такт/360`; возврат ТОЛЬКО на входе на карту
(`0x43DF48:314-317`): грядка оживает при `метка×360 + текущий_такт > 0x1517`
(≈5760 мировых тактов ≈ 7.5 мин). Тика роста в кадре нет (проверены все 7
читателей). Критично: счётчик тактов 0x84962C и архив куч ПИШУТСЯ В СЕЙВ
(`0x423CB8:22,38`).

Наш код: модель грядок канонна и константы сходятся (`loot.js:28-36,67-77,
255-265`: REGROW_CLASSES {0x51,0x52}, REGROW_TICKS 5760, ленивый возврат).
Дыра 1: единственный игровой путь сбора — окно обмена → `trade.js:356-357`
ставит taken, метка отрастания НЕ ставится никогда (тот же корень, что №14).
Дыра 2: метка в `clock.ticks`, а сейв хранит только `daylight.time`
(`save.js:290,676`); `clock.ticks` начинается с 0 на каждой загрузке страницы
(включая `respawn.js:107 location.reload()`) — после любой загрузки помеченные
грядки не отрастают часами. Канон, напротив, сохраняет счётчик тактов.

Правка: общая `lootEmptied(pile)` из №14 (грядка Q/R без денег → метка) —
впереди. Дыра 2 СДЕЛАНА 15.08.2026: `ticks` пишется в сейв (канон
`0x423CB8:22`), восстанавливается в `applySave` и — раньше — в буте, ДО
`lootSetup`, чтобы грядки стартовой карты сверялись с настоящим тактом.

ВАЖНО для lootEmptied (подтверждение игрока, 15.08): в оригинале «посадить
растение» — выбросить, например, орех в землю — превращает точку в
плодоносящую. По канону `0x4136A8` это выпадает само: метку отрастания
получает ЛЮБАЯ куча с вещью класса 'Q'/'R' без денег — в том числе
созданная игроком (`0x423360`). Нашу `lootEmptied` НЕ ограничивать
пачными грядками — предикат по классу содержимого, а не по происхождению
кучи.

**Попутно:** система зарядов работ объектов карты (+4 записи, восполнение на
входе `0x43DF48:241-243` из таблицы 0x758368) не перенесена вовсе (полей нет
в паке) — вне жалоб, но зафиксировать.

---

## F. Имена и интерфейс (№10, №22, №8, №19, №23)

**№10. Все монстры без своих названий. ПОЧИНЕНО в рабочей копии (тестер видел
старую сборку).**

Канон. Печать имени юнита — `0x43000C`: если байт +0x1A несёт бит 0x40 и
(порода & 0x7F) ≤ 0x53 — имя берётся не из записи юнита, а из таблицы пород
по указателям VA 0x45FAE0 (84 записи; дамп из exe: 0x45 «Мертвяк», 0x46
«Ичетик», 0x4A «Навья», 0x4C «Скелет», 0x4D «Паук», 0x53 «Кикимора»...).
Иначе — имя по +0xF0 из базы PTR_0046188C плюс прозвище по +0xF1. Записи 1–5
той же таблицы пород — должностные приставки «, староста»... «, воевода»
(подставляет подпись-наведение `0x432A44` через `FUN_00415190`). Вызывающие:
0x42A8F4 (экран персонажа), 0x42C2AC (торг), 0x432A44 (подпись).

Наш код. Причина была в паке: старый `map_units` ветки твари не имел — имя
всегда `names[+0xF0]+names[+0xF1]`, у тварей нули → заглушка «житель N»
(в снимке 13.08 на карте 50 «В пути» навьи лежат как «житель 455…471»).
Исправлено: `konung2/gamefile.py:846-859` (ветка породы), `:516-546`
(`BREED_NAMES_VA=0x45FAE0`). Прогон по свежему паку: 36 611 юнитов, 0 тварей
с generic-именем. Осталось: коммит, выкладка. Мелочь вдогонку: `unitCaption`
(`cursors.js:184-213`) добавляет « враг» и тварям, а канон 0x432A44 печатает
« враг» только людям чужой стороны.

**№22. «Оттар Исток» вместо «Оттар Волчий Клык». ПОЧИНЕНО в рабочей копии.**

Канон. `0x43000C`: имя — база PTR_0046188C по +0xF0, прозвище — ДРУГАЯ база
PTR_00461B70 по +0xF1; между базами (0x461B70−0x46188C)/4 = 185 указателей.
Дамп: имя №19 = «Исток», прозвище №19 = «Волчий клык», №13 «Острый клык».

Наш код. Старая строка пака — `nick = names[unit[0xF1]]` — прозвище прямым
индексом из таблицы ИМЁН: 19 → «Исток» (у Харальта 13 → «Вячеслав»).
Исправлено: `konung2/gamefile.py:549-566` (`NPC_NICKNAMES_FROM = 185`),
применено в `map_units` и `party`; свежий пак даёт «Оттар Волчий клык»,
«Харальт Острый клык» — сверено с exe.

**№8. «Текст в таблице перемешан». Скорее всего ПОЧИНЕНО; ЖИВЬЁМ (нужно фото).**

Канон. Вероятный кандидат — экран персонажа (единственный табличный экран
сверху): `0x42A8F4` печатает числа по таблице пар (x,y) VA 0x4612E4
(37 записей), подписи по VA 0x46140C (41 запись); свободный опыт — ПРАВЫМ
краем (`x − ширина строки`, `0x42A8F4:158-160`), характеристики: база правым
краем, текущая на +0x5A, «+» на +0x87.

Наш код. В сборке 13.08 два расхождения, дающих ровно «всё перемешано»:
пол шрифта 9px при сжатии координат с коэффициентом (буквы налезали) и
`free_xp` левым краем (уезжал за край окна в верхней строке). Оба исправлены:
`ui.js:1417-1422` (пол снят), `:1482-1488` (правый край); в паке добито число
подписей 34→41 (`konung2/interf.py` — семь нижних «Здоровье…Золото» раньше не
пеклись). Текущая раскладка сверена с 0x4612E4/0x46140C — расхождений нет.
Торговый экран тоже проверен против `0x42C2AC:115-146` — порядок шести чисел
совпадает. Запросить у тестера фото для окончательной привязки.

**№19. Подсветка вещей «некрасива». НЕ БАГ логики (фича канона), но отрисовка —
наша аппроксимация + одно реальное расхождение.**

Канон. Подсветка родная: `0x42FF20` — нельзя надеть → КРАСНЫЙ (0x18,0,0),
опознана и есть чары → ЗЕЛЁНЫЙ (0,0xC,0), не опознана → ЖЁЛТЫЙ (0xF,0xF,0).
Движок перекрашивает ВЕСЬ значок через предрасчитанные канальные таблицы
(0x4BEA2C/0x479D50/0x474950, прогон каждого 5-битного канала палитры —
`0x44293F`/`0x441DF9`). Жёлтая ветка в честной игре недостижима: `0x418648`
неопознанной вещи не даёт пройти «можно надеть» — она краснеет раньше.

Наш код. `ui.js:228-236` (`itemTint`) выбирает состояние канонично, но краска —
тонкий контур `drop-shadow` (`styles.css:676-678`) вместо перекраски значка.
Реальное расхождение: у торговца неопознанный товар у нас ЖЁЛТЫЙ (слово чар
в `requirementMet` не передаётся), а движок показал бы КРАСНЫЙ. Правка:
перекрашивать иконку canvas-фильтром в канонные цвета (или запечь три
канальные таблицы в пак) и передать слово чар в `itemTint`.

**№23. Броня на юнитах появляется с задержкой. ПОДТВЕРЖДЁН (наш стриминг,
в оригинале невозможен).**

Листы актёров (72 листа, 121.6 МБ) сознательно не входят в предзагрузку карты
(`world.js:120`), тянутся по требованию с приоритетом по расстоянию
(`app.js:383-430`, 6–12 параллельных закачек); `drawActor` (`actor.js:419-442`)
показывает юнита, как только доехал лист ТЕЛА, а `drawLayerFrame` (`:412`)
пропускает слой снаряжения, чей лист ещё едет — юнит выходит «голым» и
одевается по мере докачки. Направления: (1) не рисовать юнита, пока не готовы
все его слои — ближе всего к оригиналу по картинке; (2) класть листы слоёв
стартовых юнитов карты в предзагрузку; (3) минимум — вес 0 в очереди для
слоёв уже видимого юнита.

---

## Итог по 28 пунктам

**Баг подтверждён, нужна правка (13):** №1 (флаги → статы: +15 Хар/+10 Инт),
№2 (preload куч), №5 (анимация тренировки; механика есть), №12 (должность не
освобождается + утечка squad_people), №13+№18 (клон из памяти приёмышей),
№14 (гнездовые тайники и dug), №15 (юзабельные — только герой), №16 (46 не
вычёркивает из отряда), №17 (грядки: tradeFinish + clock.ticks), №20 (экран
засады), №21 (H30 = NaN), №23 (стриминг брони), №25 (оффмап-тик стройки +
пауза при 0 работников).

**Починено в рабочем дереве — довезти до тестера (10):** №3, №6, №7
(+ общая сейв-дыра: сейв на карте не снимает жителей), №4 (потеря вещи —
прогнать живьём), №8 (экран персонажа; нужно фото), №10, №22 (имена — пак),
№24 (наполовину; home стартовой паре), №26 (урон от здоровья), №27 (выброс
у кучи — прогнать живьём), №28 (на свежей сборке проверить).

**Не баг по канону (3):** №9 (охотник забирает целиком — так в оригинале),
№11 (Топор = Вын 36, Секира 83/Палица 65 — не дадутся и в оригинале;
проверить экземпляр живьём), №19 (подсветка — фича канона; наша краска
некрасива + жёлтый у торговца должен быть красным).

**Главные сквозные корни:**
1. Сейв, снятый стоя на карте, не содержит снимка её жителей
   (`mapStatePack` без `mapStateCapture`; `packFoe` без
   removed/hidden/flags/bag) и не хранит `clock.ticks` — воскрешает
   квестодателей, откатывает мешки торговцев, замораживает грядки.
2. Идентичность юнита расщеплена на `unit_N` / `mate_N` / запись памяти —
   все клоны и должностные баги отсюда.
3. Тик мира живёт только на текущей карте — канон тикает все 12 поселений
   (стройка, учёба) независимо от карты.
4. Тестер играл сборку 13.08: значительная часть жалоб уже закрыта
   непрокоммиченными правками, но без выкладки это не видно.

## Порядок работы

1. **Сейв-дыра** — СДЕЛАНО 15.08.2026 (mapStateCapture в saveState +
   clock.ticks в сейве и раннем буте; packFoe расширять не пришлось —
   снимок жителей покрывает; 2 контрактных теста, 640 passed).
   Закрывает хвосты №3/6/7/28 и половину №17.
2. **Идентичность юнита** — СДЕЛАНО 15.08.2026 (найм снимает joined-запись
   + 2 защиты от клона; 36 освобождает должность и убавляет squad_people;
   46 убавляет счётчик и вычёркивает союзника из members; H30 по слоту;
   home бойцам в выпечке + миграция старых сейвов; 640 passed). Требует
   пересборки пака (shared.json). Щель: чистка 61 только по текущей карте.
3. **Кучи** — СДЕЛАНО 15.08.2026 (`lootEmptied` по 0x4136A8 — гнездо
   живёт/грядка метится/напольная уходит, посадка растений в ту же ветку;
   dug в openPile; lootAssets + requestAsset в отрисовке; 644 passed) —
   №2/14/17.
4. **Оффмап-тик поселений** (стройка по записям всех 12, без паузы при
   0 работников; учёба туда же) — №25 + часть №5.
5. **Таблица прибавок 0x45D431** (флаги → статы) — №1 и всё семейство
   (проклятие, здоровье, Сила +300).
6. **Юзабельные для дружины** + класс 39 «Свиток ведуна» — №15.
7. **Экран засады** (encounts/sbuttle из PICS) — №20; заодно каркас родных
   экранов загрузки.
8. **Анимация тренировки** — видимая часть №5.
9. Косметика: перекраска подсветки + жёлтый→красный у торговца (№19),
   броня целиком (№23), порог в подсказке вещи (№11), хвост купца 14–31 и
   банки знахаря 0/13 (смежные из A).
10. **Выкладка**: пересборка пака Титанов (манифест сейчас от «Легенд»!),
    pytest, коммит, деплой ТОЛЬКО с разрешения, сверка sha; повторный
    прогон тестера по пунктам «починено».

## Подготовка релиза — СДЕЛАНО 15.08.2026 (выкладка ждёт разрешения)

1. Легендовская сборка отложена в `content_build_legend_20260815`;
   `content_build` посеян канонным снапшотом 13.08 (кадры героя из кеша) и
   пересобран: все 52 карты Титанов, 8814 файлов, 496 МБ,
   `knyaz2.content verify` — OK.
2. Факты выпечки сверены числами (scratchpad/check_release.py):
   манифест 52 карты (1..54), стартовая 33; в shared 6 канонных стартов,
   у ВСЕХ спутников `home` = карта старта (мир 0: Ратибор/Путята
   Медведь/Тур — 33); карта 50: «Навья»×6 во встречах (encounters, не
   units_by_world — сцены пусты по построению), заглушек «житель N» ноль;
   карта 39: «Оттар Волчий клык», «Исток» как прозвище исчез; карта 18:
   куча 54:47 (топор класса 138) и спрайт ui_154.png в паке.
3. pytest: 648 passed, 2513 subtests. ДВА УПАВШИХ — устаревшие ожидания
   чужой незакоммиченной фичи «девять героев Новой игры» (слияние с
   донором): `CreationLayoutContractTest` ждёт 91 прямоугольник (стало 94),
   `HeroLifecycleContractTest` ждёт 6 миров (голый `_hero_choices()` даёт
   9, включая донорские 155/164/154). В ИСПЕЧЁННОМ паке стартов ровно 6 —
   релиза Титанов не касается; тесты поправить вместе с фичей донора.
4. Живая проверка (сервер 8765, свежая игра за Ратибора, Борье):
   отряд из пака с home=33; `knyaz2.agent.selfcheck()` — **7/7**.
   Кадровый `knyaz2.selfcheck()` — 6/11 и на новом паке, и на снапшоте
   13.08 (одни и те же пять правил, замер с согласованной геометрией и
   снятыми юнитами) — пересборка рендер не меняла; по слову игрока,
   падения объясняются шедшей рядом сборкой. Не блокер.
5. Дальше по разрешению: коммит, выкладка, сверка sha отданных файлов
   (кэш Cloudflare), повторный прогон тестера по пунктам «починено».

## Как проверялось

Семь независимых разборов, каждый — чтением функций декомпилята целиком
плюс дампы таблиц из konung2.exe (смещение VA→raw сверено по PE-заголовку);
данные сверялись с сырыми GAME.0/QUESTS.RES (размеры блоков сошлись
побайтово). Прежние записи, которые аудит ОПРОВЕРГ: «благословение ничего
не даёт» (TESTER_2026_08_11 E2), подписи обработчиков 39/40/41 и 71/72,
«генератор наполняет только пустые места» (верно лишь для мест 0–13).
