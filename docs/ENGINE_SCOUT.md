# Разведка нетронутых функций движка (2026-08-07)

20 крупнейших функций, которых порт не касался.

## 0x0041896c — Торговля: наполнение лавки деревни товаром (случайный ассортимент по уровню героя и народу деревни)

- перенесено: no
- что делает: Генерация ассортимента деревенского прилавка. Признаки: пишет короткие индексы вещей в слоты param_1+0x3E0 (2 слота, шаг 0x1A) и param_1+0x40E (32 слота по 2 байта) — это в точности прилавки из konung2/trade.py (COUNTERS = {2:(0x3E0,0x16), 3:(0x40E,0x20), 4:(0x44E,0x27)}); создаёт записи вещей через FUN_0043b6d8 в массиве 0x6F956C (шаг 16 байт: +0 вид, +3 класс, +4/+8 износ из таблицы классов 0x45DAF8+0x08, +0xE слово чар/количества); уровень качества = байт героя +0xF3 (уровень, см. konung2/progress.py), обрезанный до 13; народ деревни = байт +0x1B отряда торговца (0x71E56C+0x18, сдвиг >>24). Единственный вызов — из 0x0043df48 (вход на карту/загрузка локации), т.е. прилавок наполняется при входе на карту.
- приоритет: Высокий, если в браузере уже есть торговый экран (trade.js работает): без этой функции прилавки деревень пусты или заполняются отсебятиной. Формулы самодостаточны (только rand и таблица классов, уже перенесённая в items.py). Понадобится уточнить таблицы выбора оружия FUN_00416e80/FUN_00416f48 (0x00416e80, 0x00416f48).
- ключевые числа: Уровень L = min(байт героя+0xF3, 13). Слоты +0x3E0 (2 шт, шаг 0x1A): rand%3+1 вещей вида 9 класса 0x53=83. Слоты +0x40E: [0..2] шанс 29% (rand%100>0x46), вид по rand%30 (<10 вид 8 база класса 66, <20 вид 7 база 63, иначе вид 6 база 60) + rand%(L/5); слово +0xE = (rand%(2*(L>>3)+2)+1) << (3*(rand%4)). [3..4] шанс 49% (rand%100>0x32): вид 0x0B, класс 30+rand%27 (30..56 — свитки/камни). [5..7] шанс 69% (rand%100>0x1E): оружие (вид 0) через FUN_00416e80(народ,L)/FUN_00416f48, износ из табл. 0x45DAF8+народ_класс*0x20; шанс чар 10% (rand%0x65<10): (rand%(L/2)+1)<<(3*(rand%5)). [8..13] шанс 49%: rand%3 → вид 2 (доспех, база 120+народ*34), вид 4 (щит, база 124+народ*34), вид 3 (шлем, база 129+народ*34); прибавка rand%(L/4+1) для доспеха, rand%(L/3+1) для щита/шлема; шанс чар 7% (rand%100<7), сдвиг 3*(rand%5). [14..31]: слоты чистятся (байт вида = 0xFF) и кладётся фиксированный набор 18 вещей: вид 7/6/8 (+чары), оружие народов (нация+1)%3 и (нация+2)%3, метательное (вид 1, база 115+народ*34, прибавка rand%(L/8+1) или rand%(L/5+1) по rand%2), доспех/щит/шлем обоих чужих народов и случайного народа. Все случайности — FUN_00442b93 (rand).

## 0x0041c944 — Мировая симуляция: отрава и таймеры юнитов, стройка и пожар, казна деревни, разбойничьи набеги

- перенесено: partial
- что делает: Мировой такт. Работает по маске кадров: (тик & 0xF)==0 — отряды и разбойники, ((тик+7) & 0xF)==0 — деревни. Признаки: вызывается из 0x00438a00 (главный игровой цикл); внутри вызов функции с сохранённым именем W_SFLB_village_raiders_n_pn_tgroup__pn_tvillage___l; ходит по массиву лагерей разбойников 0x7B2ACC (10 записей по 0x10), по 200 отрядам 0x71E56C (шаг 0x100), по юнитам 0x7B3C08 (шаг 0x100) и по 12 деревням 0x83D408 (шаг 0x4A1). Делает: (а) спавн и блуждание разбойничьих ватаг по глобальной карте 24x32 (клетка DAT_00460174 + y*4 + x*0x80, бит 0x10 байта +3 — непроходимо, младший байт — номер деревни) и грабёж деревни с сообщением игроку; (б) тик юнитов: отрава (+0x4E здоровье -= +0x52), таймер браги/оглушения +0x4A с возвратом 6 байт характеристик из +0xC6 в +0xC0; (в) тик деревень: ход стройки/пожара (счётчик = время_постройки*60/работники, таблица 0x45D848 шаг 0x10), казна деревни (+0x10) раз в игровую неделю.
- приоритет: Средний. Отрава и стройка уже перенесены (konung2/effects.py, konung2/buildings.py, effects.js, buildings.js). Осталось: казна деревни (нужна для экономики торговли) и разбойничьи набеги (заметная мировая механика). Казну стоит взять следующей — она короткая и завязана на уже перенесённые деревни; набеги — когда появится глобальная карта в браузере.
- ключевые числа: Маски тактов 0xF и (тик+7)&0xF (отрава уже покрыта тестом tests/test_items_contract.py:543). Разбойники: 10 лагерей 0x7B2ACC; шанс спавна — rand%10000 < порог из записи лагеря (+0x00); попытка похода — rand%0x3C==0 (раз в 60 тактов); 8 направлений (switch 0..7: N, NW, W, SW, S, SE, E, NE в осях x=+0x05/y=+0x07); карта 24x32; отряд-шаблон копируется в свободный из 199 слотов (поиск отряда с картой-слотом +0x8C+i, i=rand%7); добыча деревни: таблицы 0x460D74/0x460D76/0x460E14/0x460E16 (шаг 4 на деревню); сообщения 0x450190/0x4501C4, параметры DAT_00849574=10, DAT_008495EC=-номер_деревни. Юниты: отрава +0x52 вычитается из +0x4E; смерть при +0x4E<1 (флаг +0x16=0); таймер +0x4A-- и восстановление 6 байт +0xC6 -> +0xC0 (это возврат браги, см. konung2/effects.py класс 89). Деревни: доход раз в 10080 (порог 0x275F=10079 — игровая неделя 7*24*60), база = случайное от (число_жителей*10), делители по статусу деревни +0x49D (0/1/2) и флагу +0x4A0: /4 /2 /1, /8 /4 /2, /16 /8 /4; стройка: срок = табл.0x45D848[здание]*0x3C / число_свободных_работников, ступени byte+1 0..6 (после 3 — фиксированные сроки из 0x45D84C).

## 0x00436c48 — Применение особых вещей: магические камни в оружие/броню, точило, свитки перемещения, порошки навыков, квестовые предметы

- перенесено: partial
- что делает: Обработчик применения вещи вида 0x0B (волшебные камни, свитки, порошки): вход — if (*param_3 == 0x0B), дальше switch по классу (байт +3). param_1 — юнит-применитель, param_2 — вещь-цель (для камней/точила). Признаки: вызов W_SFLB_repairweapon (класс 51 — точильный камень), правки битовых групп слова +0xE записи вещи (классы 52..56 — пять магических камней, в точности как в konung2/craft.py), таймер +0x4A и обмен характеристик +0xC0/+0xC6 (класс 30 — механика браги), телепорт-свитки через FUN_00436908, видео BIRD_AVI. Вызывается из 0x421690 (уронить вещь на вещь — см. konung2/carry.py), 0x41f55c, 0x422afc. Использованная вещь помечается -1 и удаляется через FUN_00420b7c/FUN_00423538.
- приоритет: Средний. Камни и точило уже в craft.py/craft.js — ядро перенесено. Порошки навыков (пять классов, +5/+10 с капом 100) — дёшево и стоит добавить сразу в effects/craft. Свитки перемещения и квестовые классы (0x0F/0x16/0x19) переносить вместе с глобальной картой и квестами, раньше не нужно.
- ключевые числа: Камни 52..56 (биты 12-14/0-2/6-8/3-5/9-11 слова +0xE): прибавка = навык(+0xE2)>>4, уровень капается на 6, нынешний должен быть <=5, цель — вид <=8 и опознана (бит 0x80 байта +0xF снят) — уже в craft.py. Точило 51: цель вид 0. Класс 30 (0x1E): k = min(байт+0xC1 / 3, 5); +0x4A = k*30; +0xC1 -= 3k; +0xC0/+0xC4/+0xC5 += k; перед этим 6 байт +0xC0 копируются в +0xC6 (если +0x4A был 0). Классы 0/0x15: пара «0 + 0x15» превращает цель в класс 0x18 и даёт FUN_00413110(герой,100). Свитки: классы 9/10/11/12 → FUN_00436908(0x14/0x0D/0x17/0x13) — переход на карты 20/13/23/19; класс 0x16: требует вещь класса 0x18 (FUN_00434f8c), лечит 0x32, переход на карту 6, ролик BIRD_AVI; класс 0x19 — «копать клад»: карта 0x30, x 21..24, y 22..23 → цель -0x30, реж.3, точка (0x0C,0x16); карта 1, x 7..11, y 30..32 → цель -3, реж.5, точка (6,0x1A); класс 0x0F: карта <4 → цель -6, реж.2, точка (0x78,0x12), иначе -3, реж.1, (0x30,0x1E). Порошки навыков: класс 0x28 (+5 к +0xE3, кап 100), 0x2C/0x2D/0x30/0x32 (+10 к +0xE0/+0xDD/+0xE4/+0xE1, кап 100), класс 0x31: FUN_00413110(юнит, навык(+0xE2)*3). Класс 0x22: всем чужим юнитам на карте флаг +0xF9 |= 2, кроме защищённых вещью класса 0x21 (FUN_00432c30); класс 0x25: байт +0x1D = 0xFE у всех чужих; класс 0x23: раскрытие спрятанных объектов (0x794D5C и 0x7AD7E4, шаг 0x65, знак слова +0xF); классы 0x1F/0x26: своим юнитам (кроме букв 'D','V','R' и видов 3/0xB/0xC) +0x10=0, +0x16=0x28.

## 0x0042dda0 — Рендер: present кадра DirectDraw, грязные прямоугольники, курсор мыши

- перенесено: not-gameplay
- что делает: Составление и показ кадра через DirectDraw. Признаки: циклы по vtable-вызовам с кодами ошибок -0x7789FDE4 = 0x8876021C (DDERR_WASSTILLDRAWING, ожидание в цикле do-while) и -0x7789FE3E = 0x887601C2 (DDERR_SURFACELOST → Restore обеих поверхностей); клип-прямоугольники 0..0x3FF x 0..0x2FF (экран 1024x768); стирание/рисование курсора 100x100 (спрайт DAT_006C5158) в точке DAT_00849744/DAT_00849760; отрисовка трёх очередей спрайтов (0x71956C, 0x71AE6C, 0x71C76C, по 8 dword на запись) с накоплением общего «грязного» прямоугольника и финальным Blt на первичную поверхность. Единственный игровой нюанс: при режиме курсора DAT_00849650==8 (несём вещь) вместо стрелки рисуется иконка вещи — спрайт из поля +0x16 класса (таблица 0x45DB04 = 0x45DAF8+0xC), каталог INTERF 0x6B2618.
- ключевые числа: Браузеру не нужно. Единственное игровое правило — курсор режима 8 показывает иконку несомой вещи (поле класса +0x16, INTERF.RES) — уже зафиксировано в konung2/cursors.py:35 и knyaz2/web/static/cursors.js:86. Границы интерфейса, видные из клипов: игровое поле начинается с x=0x8C (140), низ панели y=0x2C4 (708) — могут пригодиться при точной вёрстке UI.

## 0x00424514 — Рендер кадра: y-сортировка и порядок слоёв (трупы → эффекты → декор → кучи → живые), плюс попутный mouse-hover hit-test

- перенесено: not-gameplay
- что делает: Отрисовка одного этажа/слоя локации внутри главного рендерера карты. Единственный caller — 0x425AA8 (в порте задокументирован как «оригинальный рендерер», konung2/res.py). Признаки: painter-сортировка по экранному Y через вёдра 16 байт на 0x84F53C (очистка 32000 байт, переполнение цепочкой в 0x85723C, счётчик 0x8A7324), вызовы отрисовщика юнита 0x425DB4, спрайт-блиттеров 0x43FFD1/0x43FBDC, hit-тестов мыши 0x427178/0x441F63 (заполняют глобалы наведения 0x849528 юнит, 0x84952C декор, 0x8496F8/0x8496FC/0x849510 куча). Порядок внутри слоя: (1) лежачие/трупы (байт unit+0x1A & 0x80), (2) пламя над горящими юнитами (проверка 0x415040; спрайт по здоровью short unit+0x4E), (3) декор слоя из 0x6AE458+слой*0xC0 (записи 12 байт: спрайт, палитра|флаг, x, y; счётчик — байт 0x844304+слой), (4) кучи вещей по клеткам сетки 0x5662BC (dword клетки: биты 0x2000 и 0x200000, биты 16–20 == слой+1; спрайт из каталога INTERF 0x6B2618[n*8]; режим подсветки 0x849610 — подложка 0x6A5598), (5) необязательный оверлей выделения (0x849604: 0x424200 рамки юнитов, 0x43F260 маски построек, 0x440788), (6) живые юниты той же вёдерной сортировкой. Списки юнитов слоя — 0x84977C+слой*800 (200 указателей), счётчик 0x86727C[слой].
- приоритет: Низкий: браузерный кадр собирается по мастеру 0x4288D4 (knyaz2/web/static/scene.js), послойная композиция юнита 0x425DB4 уже в actor.js. Стоит забрать только две геймплейно-видимые детали: размер пламени горящего юнита по порогам здоровья 401/801 и подсветку куч 64x43 по клавише.
- ключевые числа: Ключ сортировки = экранный y + высота кадра (низ ног); если байт unit+0x1A равен 0x52 ('R') или 0x49 ('I'), ключ +0x3C (60). Клип видимости: x < 0x400 (1024), x + ширина(short +0x54) > 0x8B (139), y < 0x2C5 (709), y + высота(short +0x56) >= [0x849768]. Пламя горящего: здоровье short unit+0x4E < 0x191 (401) → спрайт [0x6B2B78], < 0x321 (801) → [0x6B2A08], иначе [0x6B2BA8]. Подсветка куч (флаг 0x849610): тёмная подложка 0x40 x 0x2B (64x43) в точке (x − 0x20 + w/2, y − 0x15 + h/2). Клетка с кучей: dword & 0x2000 && & 0x200000 && ((dword & 0x1F0000) >> 16) == слой+1. Вёдра сортировки: 2000 x 16 байт {ptr, x, y, chain:short на +0xE}, диапазон Y 0..0x2FF.

## 0x004115ac — Приказы юнитов: прибытие к цели — разговор, обыск/опознание, вход в здание, переход карты; коррекция погони за движущейся целью

- перенесено: partial
- что делает: Диспетчер «шаг пути сделан / приказ дошёл» для юнита. Признаки: буфер пути — байты unit+0x00..0x0F с терминатором 0xFF; байт приказа unit+0x16 (младший ниббл — вид), цель short unit+0x10 (юниты 0x7B3B08+n*0x100, отрицательная — куча 0x7AD7E4+(−1−n)*0x65); радиус разговора 7x4; бросок опознания против навыка героя +0xE1 (навык 15 «Идентификация предметов»); обыск трупа с переносом денег (+0x26) и снятием экипировки через 0x423538. Callers 0x410A08/0x413894 — цикл действий юнитов. Ветки: вид 2 — разговор при |dр|<7 и |dс|<4 (0x4369A0, флаг диалога 0x8495F0); вид 3 — обыск кучи (деньги, бросок rand%100 < навык[+0xE1] по каждой из 42 вещей с битом 0x80 в байте +0xF записи вещи 0x6F956C[n*16], при удаче бит снимается и навык +1 до 100; экран обмена 0x424128) либо обыск лежачего (деньги +0x26, оружие short +0x50, 5 слотов +0x58, 5 слотов +0xB6 перекладываются в кучу, экран 0x43346C); вид 4 — по dword-таблице отряда +0x20+место*4: & 0xF0 == 0x90 или 0xA0 → состояние 9, иначе 10; виды 5 и 0xB → состояние 7; по умолчанию — бит клетки 0x10 запускает переход с карты (0x420900). Погоня за движущейся целью: перепрокладка пути 0x414F94, разворот направления dir<4?dir+4:dir−4, таблица обхода 0x4624B4[dir + result*8] (0xFF — нет обхода, бит 0x80 — сброс шага); атака (вид 1) прекращается, если цель мертва (байт +0x1A & 0x80) или её байт +0x17 равен 3, 0xB, 0xC.
- приоритет: Средний: ядро уже в порте (konung2/orders.py, piles.py, enchant.py; knyaz2/web/static/orders.js, combat.js, loot.js, jewels.js — радиус 7x4, бросок опознания, деньги и обмен). Не перенесены: коррекция погони по таблице 0x4624B4, остановка атаки по состояниям цели 3/0xB/0xC, ветки видов 4/5/0xB (состояния 9/10/7) — понадобятся для боёв и ИИ преследования.
- ключевые числа: Радиус разговора: строки < 7, столбцы < 4 (уже в порте: TALK_ROWS, TALK_COLS). Опознание: по каждой из 0x2A (42) вещей кучи с битом 0x80 в байте +0xF записи 0x6F956C[n*0x10]: rand%100 < навык(hero+0xE1) → бит снять, навык += 1, потолок 100. Слоты трупа: оружие short +0x50, 5 слотов short +0x58+2i, 5 слотов short +0xB6+2i, деньги int +0x26. Путь: 16 шагов, 0xFF конец; обратное направление = dir<4 ? dir+4 : dir−4; таблица обхода 0x4624B4, строка 8 байт на результат 0x414F94, 0xFF = тупик, бит 0x80 = отменить шаг. Стоп-состояния цели атаки: байт +0x17 ∈ {3, 0xB, 0xC} или флаг смерти +0x1A & 0x80. Вид 4: (dword отряда +0x20+место*4) & 0xF0 == 0x90|0xA0 → состояние 9, иначе 10; виды 5, 0xB → 7. Бит клетки 0x10 = триггер перехода.

## 0x0043a628 — Смена карты: похороны мёртвых с выпадением их вещей в кучи, архив куч между посещениями, завершение боёв и рейдов на глобальной карте

- перенесено: no
- что делает: Очистка карты при уходе с неё (param_1 — номер карты; callers: 0x420900 — переход по клетке-триггеру, 0x438A00 — главный цикл). Признаки: обход 199 отрядов 0x71E56C (шаг 0x100, поле map short +0x08, счётчик юнитов байт +0x1C), юниты 0x7B3B08 (флаг смерти +0x1A & 0x80), кучи 0x7AD7E4 (шаг 0x65), отряды глобальной карты 0x7B2ACC (10 x 16, в порте — «события/рейды»: байт +4 отряд, байт +9 сцена боя), архив куч 0x794D5C с лимитом 999 (счётчик 0x8496D0), освобождение вещей записью 0xFF в 0x6F956C[n*0x10]. Обычная локация (n < 50 и не 26..32): мёртвые юниты хоронятся (если байт разговора +0xF2 > 7 или юнит из отряда героя), их деньги (+0x26) и вся экипировка (оружие +0x50, 5+5 слотов +0x58/+0xB6, 42 слота мешка +0x62) ссыпаются в кучу клетки через 0x423360, при переполнении кучи вещь уничтожается; в записи деревни (0x43BF08) 5 ссылок на юнитов (+0x3D0+2i) корректируются, т.к. массив юнитов сжимается memmove; кучи карты складываются в архив 0x794D5C (слот — short +0x0D кучи, отрицательный = новая запись). Боевые сцены (26..32 морские, >= 50 случайные встречи): вещи куч не сохраняются, а освобождаются; мёртвые вычищаются из отряда игрока (0x420D5C + memmove, пересбор панели 0x4308E0); отряд глобальной карты выводится из боя (байт +9 = 0xFF), при полном разгроме обнуляется (байт +4 = 0), выживший возвращается на домашнюю карту (party.map short +0x08 = [0x71E474 + отряд*0x100]); временный отряд-засада 0x72AC6C распускается и зануляется (0x100 байт).
- приоритет: Высокий: прямое продолжение уже перенесённых куч (0x423360 в konung2/piles.py) и смертей (units.js/hero.js). Без него лут мёртвых пропадает, кучи не переживают смену карты, бои на глобальной карте не завершаются. Переносить вместе с механикой выхода с карты.
- ключевые числа: Классы карт: обычные n < 0x32 (50), кроме 0x1A..0x20 (26..32) — боевые/морские сцены; случайные встречи >= 0x32. Труп хоронится, если байт разговора unit+0xF2 > 7 (иначе квестовый лежачий остаётся, ср. CORPSE_TALK_BELOW=8 в orders.py) или юнит из отряда героя. Слоты выпадающей экипировки: short +0x50, 5 x short +0x58+2i, 5 x short +0xB6+2i, 42 x short +0x62+2i; деньги int +0x26 → в деньги кучи short +0x0F. Переполненная куча → вещь освобождается: байт 0x6F956C[n*0x10] = 0xFF. Архив куч: 0x794D5C, запись 0x65 (101) байт, лимит 999, счётчик 0x8496D0; слот в архиве — short кучи +0x0D (отрицательный = ещё не в архиве); при переполнении архива вещи кучи уничтожаются. Рейды 0x7B2ACC: +4 = 0 (разбит) либо возврат домой party.map = [0x71E474 + отряд*0x100]; +9 = 0xFF (вышел из боя). Деревня: 5 ссылок на юнитов short +0x3D0+2i, равные удалённому — в 0, большие — минус 1 (сжатие массива юнитов). Отряд-засада: блок 0x100 байт на 0x72AC6C, карта — short [0x72AC72+2].

## 0x00430df4 — UI прокачки: экран характеристик и навыков с кнопками +/−, предел веса, вместимость дружины, портрет-кукла

- перенесено: partial
- что делает: Отрисовка экрана персонажа «Характеристики» (param_1 = направление листания страниц, подсвечивает стрелки). Признаки: герой [0x84951C]; печать 6 характеристик hero+0xC0+i (подписи из массива указателей 0x46144C) и 20 навыков hero+0xD2+i; свободный опыт short hero+0x48 в шапке; кнопки «+» рисуются по проверкам 0x4131FC (характеристика: цена 2, потолок 150) и 0x413268 (навык: цена 1, потолок по Обучаемости) — обе уже описаны в порте; флаги отката 0x8442D8 (6 шт.) и 0x8442E0 (20 шт.) — «повышено сейчас»: значение печатается красной палитрой 0x5F744A и рисуется «−»; строка веса по формуле из Выносливости hero+0xD1; строка размера дружины из текущей Харизмы hero+0xCC; кукла героя: юнит рисуется полным отрисовщиком 0x425DB4 в буфер 0x5662BC (временно занулён на 0x28000) и блитится квадрат 0xA0 в (DAT_4622D4, DAT_4622D8); стрелки листания — спрайты INTERF 0x6B2618 номера 0x1FC..0x1FF.
- приоритет: Низкий по экрану (браузер рисует свой UI), но из него надо забрать одну непортированную формулу: вместимость дружины = (текущая Харизма >> 4) + 1 с потолком 9 — в порте вместимость сейчас читается из записи отряда (gamefile.py PARTY_CAPACITY_AT), а пересчёт от Харизмы отсутствует. Остальные правила уже в konung2/progress.py, carry.py и knyaz2/web/static/progress.js.
- ключевые числа: Вместимость дружины: ((байт hero+0xCC) >> 4) + 1, максимум 9 (hero+0xCC — текущая Харизма, i=0 из CURRENT_AT=0xCC; согласуется с девятью местами отряда в ui.js). Предел веса: ((байт hero+0xD1) * 1000 / 3 + 20000) * [0x4525BE], печать форматом "%4.1f" (граммы → кг; совпадает с konung2/carry.py WEIGHT_BASE/PER_STAMINA/DIVISOR = 20000/1000/3). Раскладка: подписи прижаты вправо к x=0x366, значения к x=0x398, «+» на x=0x3B6, «−» на x=0x3CA; характеристики с y=0x82 шаг 0x14, навыки с y=0x159 шаг 0x14, вес y=0x10E, дружина y=0x122; кукла — квадрат 0xA0 x 0xA0 (клип 0..0x9F). Флаги «повышено в этом заходе»: 0x8442D8[6] и 0x8442E0[20].

## 0x0041d954 — Зелья и отрава (питьё, намазывание яда/масла, временные баффы браги/зелья/слезы/эликсира)

- перенесено: partial
- что делает: Применение зелья: switch по номеру класса (байт +0x03 записи предмета 0x6F956C), классы 'T'..'\\' = 84..92. param_1 — юнит, param_2 — целевой предмет (может быть NULL = пить самому), param_3 — запись зелья. Ветки: 84 здоровье/2 (min 1); 85 лечение; 86 яд (в юнита +0x52 или в предмет +0x0C, годятся вид 0 и 0x0C); 87 масло (ставит +0x02 боеприпаса в 0); 88 противоядие; 89-92 временные зелья с таймером unit+0x4A и сохранением 6 байт статов +0xC0..0xC5 в +0xC6 (FUN_00442bc6). После действия класс становится 83 (пустая банка, param_3[3]='S'), банка кладётся в мешок (FUN_00423538), звук 0x11 (FUN_0042d660), перекраска портрета FUN_004305A4. Если первый байт цели '\t' — уходит в W_SFLB_mixmagics (0x41B930, варка). Опознаётся по классам 0x54..0x5C и по вызову mixmagics.
- приоритет: Высокий. Механика уже наполовину в порте и покрыта контрактным тестом (test_items_contract.py:492); осталось перенести точные формулы 85/86/88 (константы 100.0/0.1/0.01/10.0 теперь известны) и четыре временных зелья 89-92 (длительности *30/*60 тактов, дельты статов). Перед фиксацией сверить по asm две FP-последовательности, съеденные Ghidra (итог лечения и k при слабой крепости).
- ключевые числа: Потолок здоровья 0x640=1600 (unit u16 +0x4E). Двойные константы в коде (проверено по exe): 0x4501EB=100.0, 0x4501F3=0.1, 0x450203=0x45020B=0x45022B=0.01, 0x450213=10.0, 0x45021B=0.1. Лечение (85): расход = (100.0 − здоровье/16)*0.1, но не больше остатка крепости (+0x04 f32); крепость −= расход; банка пустеет при крепости ≤ 0.01; новое здоровье округляется и режется по 1600 (точная FP-последовательность съедена декомпилятором — сверить по asm; расход*160 = ровно недостающее здоровье). Масло (87): нужна крепость ≥ 10.0, цель вид 0x0C, пишет 0 в +0x02. Яд (86): если в предмет и округлилось в 0 при крепости ≥ 0.01 — ставится 1. Противоядие (88): расход = отрава(+0x52)*0.1, ниже нуля отрава не уходит, банка пустеет при ≤ 0.01. Брага (89): k = ум(+0xC1)/3 (если крепости меньше — меньше), таймер +0x4A = k*0x1E (30), ум −3k, +0xC0/+0xC4/+0xC5 по +k. Зелье (90): таймер k*30, +0xC1/+0xC4/+0xC5 по +k, здоровье заново (cap 1600). Слеза (91): глобаль 0x849610=1, таймер k*30, облик unit+0xF8 = round(...). Эликсир (92): требует k ≥ 6, таймер k*0x3C (60), +0xC4 −5k (min 1), здоровье −k*0xA0 (160), но не ниже 0xA0; ум через FUN_00413110. В порте (konung2/effects.py, knyaz2/web/static/effects.js) классы и семантика 84-88 есть, но: heal в JS лечит всегда до максимума (без формулы расхода), классы 89-92 возвращают null («остальные ещё не читаны»), пороги 0.01/10.0 и множители 0.1 нигде не записаны.

## 0x0042ebd0 — Точка входа: окно, конфиг, DirectDraw, AVI, цикл сообщений — источник ТАКТА игры

- перенесено: not-gameplay
- что делает: WinMain игры: RegisterClassA (класс окна «Konung» = строка s_edKonung_004627f6+2, иконка GAME_ICON), CreateWindowExA 0x400x0x300, чтение KONUNG2.CFG, инициализация DirectDraw (FUN_0042C98C(0x4000000), FUN_0042CA1C, FUN_0042CE0C), звука (FUN_0043C228), загрузка MP.DLL с экспортами CreateAviWnd/FreeAviWnd/isPlayingAviWnd/StopAviWnd (видеоролики), ShowCursor(0) и главный цикл сообщений PeekMessage/GetMessage. Оконная процедура — LAB_0042F22C.
- приоритет: Сам WinMain переносить не надо (окно/DirectDraw/AVI браузеру не нужны), но шаг кадра 77 мс (0x4D) стоит зафиксировать в konung2/ как канонную временную базу: сейчас knyaz2/web/static/app.js крутится на requestAnimationFrame и нигде не привязан к 13 fps оригинала — все счётчики тактов (стройка, отрава, зелья k*30) без этого имеют другую длительность в секундах.
- ключевые числа: Единственные канон-числа для порта: (1) главный тик — SendMessageA(WM_USER=0x400) когда timeGetTime() − прошлый > 0x4D мс, то есть шаг кадра 77 мс ≈ 13 fps; от него считаются ВСЕ покадровые счётчики порта (мировой такт «каждый 16-й кадр» = раз в ~1.23 с); (2) двойной щелчок — своё сообщение 0x401 по GetDoubleClickTime(); (3) пауза: при глобали 0x8495C4 != 0 цикл встаёт на блокирующий GetMessage — тиков нет; (4) формат KONUNG2.CFG: 10 dword подряд — 0x84958C, 0x849598, 0x84959C, 0x8495B4, 0x8495DC, 0x8495E4, 0x8495F8, 0x849604, 0x849614, затем слово языка: биты 0-1 — тексты (0 -> таблица 0x5EE348, 1 -> 0x5F744A, иначе 0x60054C), биты 2-3 — озвучка по той же тройке; без файла: первый dword 0, остальные 1, оба языка -> 0x5EE348.

## 0x0041e280 — Инвентарь: положить/взять вещь в гнездо экипировки

- перенесено: yes
- что делает: Щелчок по гнезду экипировки: param_1 — номер гнезда (0 рука, 1 метательное, 2 доспех, 3 шлем, 4 щит, 0xC боеприпас). В режиме курсора 2 (несём вещь нет) — ВЗЯТЬ из гнезда через FUN_0041DFE0 (код 1 для боеприпаса +0x50, код 5 для снаряжения +0x58). Иначе — ПОЛОЖИТЬ несомую вещь (глобаль 0x8449E0, запись 0x6F956C+idx*16): проверка требований FUN_00418648 (провал — сообщение 0x45024C + отмена FUN_0042944C), совпадение вида (+0x00) с гнездом, двуручное (группа класса >0xC из таблицы 0x45DB08, слово +0x02) сгоняет щит (+0x60) в мешок, в щитовое гнездо ложится и одноручное при навыке unit+0xD3, самострелу (группа 0x15) нужны болты (класс >0xCD), луку стрелы; вытесненная вещь уходит в мешок (FUN_00423538) или на землю (FUN_00423360), не вышло — отказ. Затем курсор в режим 2, пересчёт юнита FUN_0041C494, звук FUN_0042D660 (0x13, а для видов 2..4 — 2), обновление панели FUN_0043096C.
- приоритет: Низкий — механика перенесена и задокументирована (policy konung2_exe_0x41dfe0_0x41e280_0x423218_0x421690). Можно при случае добавить выбор звука 0x13/2, больше здесь брать нечего.
- ключевые числа: Всё уже в konung2/carry.py и knyaz2/web/static/carry.js со ссылкой на VA 0x41E280: гнёзда 0/1/2/3/4/12, двуручное с группы 0x0D, самострел группа 0x15, болты классы > 0xCD (в 0x417BD8 подтверждено: стрелы 202-204, болты 206-207), навык левой руки unit+0xD3, вытеснение мешок->земля. Не перенесена только косметика: выбор звука (код 0x13, для доспеха/шлема/щита — 2) и правка гнезда стрельбы (+0x50/+0x58/+0x60 обнуление слова при вытеснении).

## 0x00417bd8 — Экономика поселений: производство ремесленником стрел/болтов/национального снаряжения и раздача жителям

- перенесено: no
- что делает: Работа ремесленника поселения. Вызывается из мирового такта 0x41C944 (каждый 16-й кадр) для каждой записи поселения (0x83D408, шаг 0x4A1). Есть мастер (u16 поселение+0x3D6 — индекс юнита, его навык — байт unit+0xE3), режим не 7 (глобаль 0x8495F0): если сейчас ничего не куётся (байт +0x49F = 0xFF), выбирает из 39 (0x27) видов продукции первый по навыку доступный со свободной ячейкой склада (+0x44E, u16 x 39) и запускает производство (таймер dword +0x08); если всё наделано — с шансом по rand()%8 раздаёт готовое юнитам своего отряда (байт +0x02 — номер отряда 0x71E56C, юниты 0x7B3C08 шаг 0x100): живым (!(+0x1A & 0x40)), прошедшим проверку требований FUN_00418648; боеприпас — только имеющим метательное (u16 unit+0x5A) в пустое гнездо +0x50, снаряжение — в гнездо по виду, с заменой худшего (сравнение колонки +0x0C таблицы классов, старая вещь освобождается байтом 0xFF). Если куётся — таймер −1, на нуле FUN_0043B6D8 выделяет запись предмета и заполняет её; мастер растёт в навыке (cap 100).
- приоритет: Средний. Ни одного упоминания 0x417BD8/этих таблиц в konung2/ и в вебе нет — «живые деревни» (жители со временем вооружаются, склады мастерских наполняются) целиком не перенесены. Браться стоит после дочитывания таблицы классов (колонки +0x0C «качество» и +0x10 «сложность/прочность» пока не описаны в heroes.py) и после сверки двух FP-формул по asm; тянет за собой FUN_00435214 и FUN_0041B750.
- ключевые числа: 39 (0x27) видов продукции: ячейки 0-2 — стрелы классов 202/203/204 (i−0x36 как u8: 0xCA..0xCC), 3-4 — болты 206/207 (0xCE, 0xCF; порог болтов 0xCD=205 сходится с carry.py), обе категории — вид 0x0C пачками по 0x1E=30; ячейки 5-38 — снаряжение классов base+(i−5), base = культура*0x22(34) + 100, культура = байт +0x1B отряда поселения (три народа: классы 100-133, 134-167, 168-201). Порог производства: dword-колонка +0x10 таблицы классов (0x45DB08, шаг 0x20; в декомпиляте видна как 0x45F438/0x45F458/0x45DA58 — все = +0x10 mod 0x20) ≤ навык мастера (unit+0xE3, навык номер 17, cap 100). Время производства = FP-выражение из порога*0x3C (60) и (навык/10 + 1) — судя по операндам порог*60/(навык/10+1), сверить по asm. Прочность откованного = та же колонка +0x10 как float (в +0x04 и +0x08 записи), вид предмета по классу — FUN_0041B750. Рост навыка после вещи: round(f(время*2 + навык² + 10)) — f съедена декомпилятором (похоже на sqrt), сверить по asm. Раздача: флаг байт +0x49D поселения, доп. условие FUN_00435214(байт +0x03)==0, порог броска rand()%8 из байта +0x4A0: {0->7, 1->6, иначе 4} при одном флаге и {0->6, 1->4, иначе 0} при другом (байты +0x49D/+0x4A0, сверить точную пару по asm). rand — MSVC LCG 0x41C64E6D/0x3039, (seed>>16)&0x7FFF (VA 0x442B93). Текущая продукция: байт +0x49F (0xFF — простой), таймер dword +0x08.

## 0x00415b20 — Агро/скриптовая активация отрядов: зона-триггер (на отряд героя, на отряды карты, на отряд поселения) и удержание 'сцепки' пока идёт бой-контакт.

- перенесено: no
- что делает: Проверка триггеров отряда NPC за мировой такт. Признаки: обход таблицы отрядов 0x71E56C (шаг 0x100, индексы 1..199) и таблицы юнитов 0x7B3C08 (шаг 0x100); везде один и тот же фильтр 'боеспособного' юнита — flags(+0x1A)&0x80 (труп) и kind(+0x17) не из {3, 0x0B, 0x0C} (те же kind в konung2/gamefile.py помечены как 'не ставятся в клетку сетки'); прямоугольная зона [party+0x0C..+0x10] x [party+0x14..+0x16]; при срабатывании зовёт 0x4159DC (активация отряда + тревога поселения через флаги +0x49C — она упомянута в konung2/buildings.py и combat.js). Второй режим: пока флаг party+0x1D=1 ('сцеплен'), парами проверяет контакт юнитов своего отряда с отрядом party+0x06>>16 через 0x410784; если контакта больше нет — снимает флаг и перезапускает всех юнитов (0x416E24). Единственный caller 0x41C944 — большой апдейт мира из главного цикла карты 0x438A00.
- приоритет: Переносить вместе с системой отрядов NPC и событий/рейдов (T_EVENTS в gamefile.py) — сейчас в веб-клиенте отрядов NPC нет, есть только тревога-следствие (combat.js onVillageAlarm, VA 0x4159DC). Без этой функции не заработают засады и сюжетные стычки.
- ключевые числа: Отряды 0x71E56C шаг 0x100 (200 шт., обход 1..199); юниты 0x7B3C08 шаг 0x100; счётчик юнитов отряда — байт +0x1C (читается как int+0x19>>24); фильтр юнита: +0x1A&0x80==0 и kind(+0x17) != 3, 0x0B, 0x0C. Флаги триггера party+0x1F: 0x01 — реагировать на отряд героя (_0084950C), 0x04 — на любой отряд текущей карты (_008496C8) с тем же флагом 0x04 и ненулевым +0x1C, 0x08 — только когда у отряда героя +0x1D==1, 0x80 — на отряд поселения (_00849538, номер отряда — байт +2). Зона: строки party[6]..party[8] (смещ. 0x0C и 0x10), столбцы байты +0x14..+0x16. party+0x1D — флаг 'сцеплен', party+0x06>>16 — номер сцепленного отряда. Режим party+0x1E&1: ждать, пока ни у одного юнита отряда героя (unit+0x16 & 0x0F)==1, потом снять +0x1D. При активации 0x4159DC: party+0x1F |= чужой(+0x1E)&0x4F; тревога: если сторона поселения == цель, флаги |= 0x15, иначе |= 0x05 (байт поселение+0x49C).

## 0x00446397 — CRT printf/vsprintf — форматирование строк.

- перенесено: not-gameplay
- что делает: Обработчик одного спецификатора формата printf из CRT: switch по ASCII-кодам 'd'(0x64), 'i'(0x69), 'o'(0x6F), 'u'(0x75), 'x'(0x78), 'X'(0x58), 'c'(0x63), 's'(0x73), 'S'(0x53), 'e/E/f/g/G', 'p'(0x70), 'P'(0x50); читает varargs (param_2 — указатель va_list), учитывает модификаторы в байте флагов +0x14 (0x10='h', 0x20='l', 0x80=far-указатель), ширину (+0x04), точность (+0x08), знак/паддинг ('-' 0x2D, '+' 0x2B, ' ' 0x20, '0' 0x30, '0x'-префикс). Callee 0x448E75/0x448EC5 — itoa по основанию, 0x4468C8 — перевод в верхний регистр. Caller 0x445C41 — цикл разбора форматной строки. Это рантайм компилятора (Watcom), не игровой код.
- приоритет: Не переносить.
- ключевые числа: Не нужны: в браузере это штатное форматирование JS. Основания 10/16/8, флаги 'h'=0x10, 'l'=0x20 — только для понимания чужих вызовов.

## 0x00415238 — Спавн/телепорт отряда на карту: вожак в клетку прибытия перехода, спутники — случайной формацией вокруг.

- перенесено: partial
- что делает: Расстановка отряда на карте прибытия. Признаки: работает с сеткой 0x5662BC (клетка u16: &0xFFF — ссылка на юнита+1, &0xF000 — флаги; шаг строки 0x140 u16 = 0x280 байт — ровно как в konung2/grid.py, VA 0x410158), границы 0<row<0xFF и 0<col<0x9F; сначала чистит ссылки своих юнитов из клеток, затем ставит вожака в клетку (party+0x0C, party+0x14) с направлением party+0x18, а остальных — случайным веером вокруг: 8 направлений x 6 слотов из таблицы смещений 0x461BC4 c учётом чётности строки (гекс-сетка), начиная с направления 'лицом от входа' (facing<4 ? facing+4 : facing-4). Использует rand (0x442B93). Пишет юниту: +0x12/+0x13=row, +0x14/+0x15=col (совпадает с T_UNITS в konung2/gamefile.py), +0x17=0xFF, +0x18=направление, +0x16&=0xF0, u16 +0x10=0; клетка |= индекс_юнита+1. Callers: 0x438A00 (главный цикл) и 0x43DF48 (вход на карту, уже опознан в konung2/items.py и loot.js).
- приоритет: Стоит перенести при доводке переходов между картами: сейчас units.js/spawnCompanion ставит спутников просто в ряд справа от героя (col+1+index) — это нарушает канон. Нужно вытащить таблицу 0x461BC4 (8x6x2 смещений на две чётности) из exe и повторить порядок обхода с rand.
- ключевые числа: Сетка 0x5662BC: клетка = base + row*0x280 + col*4 (по u32) или u16 c шагом строки 0x140; занято = (клетка & 0xFFF) != 0; ссылка = индекс_юнита+1. Границы посадки: 0 < row < 0xFF, 0 < col < 0x9F. Таблица смещений формации: dx = s8 по адресу 0x461BC4 + slot*2 + parity*0xC0 + dir*0x18, dy = 0x461BC5 + то же (в декомпиляте int по 0x461BC1/0x461BC2 и >>24); parity = row & 1 (чёт/нечет строки гекс-сетки); dir 0..7, slot 0..5. Алгоритм: dir0 = facing±4; если rand()%2 — идти с dir0-1 в другую сторону; старт слота rand()%6; первый свободный по (клетка&0xFFF)==0 — сажаем. Если отряд героя и тихая расстановка (param_3==0): спутнику unit+0x16 = 0x10 (бит 'держаться за вожаком', см. units.js ORDER_FOLLOW). Направление прибытия копируется из перехода: T_EXITS.facing -> party+0x18.

## 0x00428b88 — UI прицеливания: какой из девяти курсоров показать над миром (бить/нельзя/говорить/поджечь/взять/переходы).

- перенесено: partial
- что делает: Выбор курсора мыши над окном мира (вызывается из 0x42DDA0 — отрисовка курсора). Признаки: границы окна мира 0x8B < mx < 0x400, my < 0x2C5, ширина левой панели 0x8C=140; пиксель->клетка через 0x43B9B0 (уже в konung2/grid.py и world/geometry.py); клетка с (люб.&0xFFF)!=0 -> 1 'нельзя'; бит клетки +1 & 0x10 -> поиск в таблице переходов 0x7B2B6C шаг 17 байт (в точности T_EXITS из konung2/gamefile.py): to_map==-2 -> основной курсор, -1 -> 6 'на глобальную карту', иначе 7 'на другую карту', и номер цели кладётся в _00849700; поджог: под мышью постройка (_00849534, бит +0x22&1 'горючая') и среди 9 выбранных (0x840B94, флаг юнита +0x19&4) есть стрелок с маслом (unit+0xEB>>24==1), снарядом (+0x50) и боезапасом (+0x5A), чей снаряд проходит по таблице классов 0x6F956E (шаг 0x10) -> 4 'поджечь'; юнит под мышью (_00849528): своя сторона (+0x1B) -> основной/3, чужая при боеспособных выбранных -> 0 'бить', иначе 1/3; труп (+0x1A&0x80) -> 3/5; вещь под мышью (_0084952C) -> 5 'взять'. Работает только в основном режиме _00849650==2.
- приоритет: Низкий — порядок проверок и значения уже перенесены (konung2/cursors.py, policy konung2_exe_0x428b88_0x43c228; knyaz2/web/static/cursors.js). Осталось довести до канона: 'нельзя' над занятой клеткой (&0xFFF), поджог перебором девяти выбранных с проверкой +0xEB/+0x50/+0x5A вместо флагов героя.
- ключевые числа: Границы окна мира: 139 < mx < 1024, my < 709 (0x2C5), нижняя граница спецрежима 0x27E; смещение мира: мышь+камера-0x8C по X. Коды курсоров: 0 бить, 1 нельзя, 2 основной, 3 говорить, 4 поджечь, 5 взять, 6 глобальная карта, 7 переход (совпадает с konung2/cursors.py). Переходы: таблица 0x7B2B6C, 250 записей по 17 байт, to_map: -1/-2/номер. Поджог: до 9 выбранных из 0x840B94; условия: юнит выбран (+0x19&4), масло unit+0xEB>>24==1, снаряд u16+0x50!=0, запас u16+0x5A!=0, байт свойства класса снаряда (0x6F956E + класс*0x10)==0. Бит перехода клетки: байт+1 & 0x10.

## 0x004171cc — Постройки: стройка и пожар — синхронизация постройки с объектом карты (спрайт, проходимость клеток, гибель юнитов в горящем здании)

- перенесено: partial
- что делает: Обновляет объект карты при смене состояния постройки. Единственный caller — 0x41C944 (мировой такт стройки/пожара, задокументирован в konung2/buildings.py). Вход — запись постройки поселения (байт +0 вид, байт +1 состояние, word +2 номер объекта карты < 0x1E). При состоянии 3 (достроена) чистит юнит-поле клеток объекта (проход по 0xA000=40960 клеткам массива 0x5662BC). При состоянии 5 (пожар) делает клетки непроходимыми (юнит-поле = 0xFFF) и убивает всех застигнутых внутри: номер 1 — герой (_DAT_0084951C: hp +0x4E = 0, позиция 0xFF/0x9F, смерть через 0x4305A4 и 0x416A00), прочие — юниты 0x7B3C08 stride 0x100 (hp = 0, смерть 0x420D5C, вычистка из 5 слотов партии героя +0x3CE/+0x3D0, декремент населения деревни по 0x71E585 + деревня*0x100 + 3). Затем пересчитывает спрайт объекта: новый = (word из таблицы видов 0x45D844[вид*0x10 + 2]) + состояние — та самая формула «спрайт вида + состояние»; ведёт refcount спрайтов (0x841CE4[спрайт+0x1E]) и очередь подгрузки 0x842CC4; копирует поля шаблона спрайта (0x75835C, stride 0x100) в запись объекта карты (0x834768, stride 0x24), выбирает случайную фазу анимации rand % (байт шаблона +0xFD) и случайные стартовые кадры оверлеев из пар min/max таблицы 0x45FFF0/0x45FFF4 (stride 8).
- приоритет: Средний. Лестница состояний и формула спрайта уже в knyaz2/web/static/buildings.js (там прямо цитируется VA 0x4171CC). Не перенесено геймплейное: непроходимость горящего здания (0xFFF в клетках) и гибель юнитов внутри — понадобится, когда в браузере заработают пожары построек с юнитами; часть про refcount/очередь подгрузки спрайтов — not-gameplay, переносить не надо.
- ключевые числа: спрайт = базовый_номер_вида (word по 0x45D844 + вид*0x10 + 2) + состояние (0..6); максимум 30 объектов карты (0x1E); клетка карты (0x5662BC, dword): биты 16-20 = номер объекта + 1, биты 0-11 = номер юнита (0 пусто, 0xFFF непроходимо), очистка низа клетки маской & 0xF000; состояние 5 (пожар): клетки |= 0xFFF, все юниты внутри умирают (номер юнита 1 = герой; остальные = 0x7B3C08 + (n-1)*0x100); запись объекта карты — 36 байт (0x24), stride шаблона спрайта 0x100; случайные кадры оверлеев: min/max по 8 байт от 0x45FFF0/0x45FFF4, значение = min + rand % (max - min + 1)

## 0x0041c494 — Зачарование вещей и украшений: пересчёт текущих характеристик, брони, силы удара, точности и здоровья от надетого

- перенесено: partial
- что делает: Пересчёт производных характеристик юнита от снаряжения — вызывается 13 функциями после любой смены вещей (equip/unequip, украшения 0x41E8D8 и т.д.). Обнуляет аккумуляторы (memset 0x841CA4, 0, 0x40 и memset 0x8442F4, 1, 0x10), проходит 5 гнёзд снаряжения (word с unit+0x58) и 5 украшений (word с unit+0xB6), для каждой ОПОЗНАННОЙ вещи (бит 0x8000 слова +0x0E снят — в коде байт +0x0F & 0x80) раскладывает слово прибавок +0x0E на пять 3-битных уровней и суммирует величины из таблиц; отдельно byte +0x01 вещи != 0xFF даёт вторую таблицу прибавок через 0x41844C (в стартовых мирах выключена). Затем: здоровье +0x4E = бонус*16 (+ база word +0x4C по флагу), cap 0x640; шесть текущих характеристик +0xCC..+0xD1 = база +0xC0..+0xC5 плюс/вместо бонуса (флаг-байт 0x8442F4[i]: 0 — заменить, 1 — сложить), отрицательное — в 0, Выносливость (+0xD1) минимум 1; поля +0x42 (броня), +0x44 (сила удара), +0x46 (точность) = аккумуляторы; байт +0xFA = биты 1/2/4 «прибавить, а не заменить» для этих трёх полей. Всё это дословно описано в konung2/enchant.py.
- приоритет: Низкий — ядро уже перенесено: knyaz2/web/static/jewels.js реализует раскладку слова прибавок, неопознанность и текущие характеристики (цитирует VA 0x41C494), konung2/enchant.py фиксирует всю политику. Остаётся хвост: применение бонусов к броне/удару/точности в боевых расчётах браузера и потолок здоровья 1600 при бонусах — проверить при доводке боя.
- ключевые числа: слово прибавок — u16 по +0x0E записи предмета (stride 0x10, база 0x6F956C); бит 0x8000 = не опознано (прибавок нет); маски секций: 0x7000>>12 броня (таблица 0x45D524), 0xE00>>9 сила удара (0x45D584), 0x1C0>>6 Ловкость (0x45D5E4), 0x38>>3 Сила (0x45D644), 0x7 Выносливость (0x45D6A4) — записи по 0x10 байт, величина = word по смещению +2 записи уровня; характеристики +1..+6, броня/удар +4..+48; здоровье: бонус * 16, потолок 0x640 = 1600 (совпадает с konung2/effects.py HEALTH_MAX); Выносливость минимум 1; биты байта unit+0xFA: 1 броня, 2 сила удара, 4 точность — «сложить со своим», иначе «заменить»; гнёзда: 5 слов с +0x58, украшения: 5 слов с +0xB6

## 0x00429b2c — Анимации юнитов и озвучка действий (голоса народов, крики боли и смерти, звуки выстрела и удара)

- перенесено: partial
- что делает: Старт действия юнита: выставляет анимацию и проигрывает звук действия. Callers — 0x416740 и 0x416c84 (постановка действия юниту). Ветвится по флагу unit+0x1A бит 0x40: люди против зверей. Люди: запись анимации из заголовка файла героев 0x844B4C (entry = действие*0x180 + направление*0x30, направление = байт +0x18), указатель кадров через таблицу 0x84694C, счётчик +0x56 = 0x96, скорость кадра +0x54 = 0x100. Звери: блок 0x72CCAC + тело*0x1728 (тело = байт +0xFC), entry = направление*0x26 + действие*0x130 + 8, кадры 0x72D3D4 + тело*0x1728 + idx*8, скорость кадра копируется из данных кадра (4 байта) — та же раскладка, что в konung2/creatures.py (VA 0x4267B8). Задержка действия +0xFD = байт-таблица 0x45FE90[действие] - unit+0x1D (у зверей для действия 1 — таблица 0x462734[тело*4]). Звук выбирается по действию и народу/телу и играется через 0x42D660 с громкостью/панорамой от клетки юнита (байты +0x12/+0x14 через 0x43BC20/0x43BC74). Финал: +0x17 = номер действия, +0x1C = 0 (текущий кадр), +0xFE = число кадров.
- приоритет: Высокий для озвучки: вся таблица «действие -> звук» нигде в порте не зафиксирована (knyaz2/web/static/audio.js содержит только выбор музыки, VA 0x437F48), а без неё бой в браузере немой. Раскладка кадров зверей уже в konung2/creatures.py, стойки — в actor.js; из анимационной части стоит забрать тайминги (задержка 0x45FE90 - unit+0x1D, счётчик 150, скорость 0x100).
- ключевые числа: ЛЮДИ, звук по народу (байт +0xFC): действия 0/6/0x10/0x12 — с шансом 4 из 100 (rand%100 >= 0x60) голос народ*8+0x23, иначе тишина; 1/0x11 — звук 13 только у героя (_DAT_0084951C); 2 (смерть) — народ*8+0x24; 3/0xB/0xC (ранение) — народ*8+0x22; 4 (выстрел) — боеприпас (слот +0x5A): вид класса 0x15 -> звук 1, иначе 5; 8 (удар) — оружие (слот +0x58): вид 0xD -> 16, вид 0xF -> 4, иначе 10 (вид = word +2 записи класса, таблица классов 0x45DB08 stride 0x20). ЗВЕРИ: 0 — шанс 4/100 тело*8+0x53; 1 — задержка 0x462734[тело*4], с шансом 24/100 (rand%100 >= 0x4C) тело*8+0x53; 2 — тело*8+0x54; 3/0xB/0xC — тело*8+0x52; 5 — тело*8+0x50. Тайминги: задержка = 0x45FE90[действие] - unit+0x1D; счётчик действия 0x96 = 150; скорость кадра людей 0x100 (8.8 fixed point). Анимация людей: 0x844B4C + действие*0x180 + направление*0x30; зверей: 0x72CCAC + тело*0x1728 + направление*0x26 + действие*0x130 + 8

## 0x0043f67e — Рендер: полупрозрачный вывод светлых кадров построек в 16-битном видеорежиме

- перенесено: not-gameplay
- что делает: RLE-блиттер спрайта с полупрозрачностью в 16-битном режиме. Вход: поверхность, заголовок спрайта (ushort ширина, ushort высота, затем ushort-смещения строк; строка — байтовый RLE: n>0 нарисовать n слов-пикселей, n<0 пропустить n&0x7F, 0 конец строки), координаты x,y. Каждый пиксель смешивается с фоном по таблицам: новый = src + table1[(dst & 0xFFE0)*2 + lut[src]] + table2[(dst & 0x1F)*0x40 + lut[src]] — разложение RGB565 на красно-зелёную и синюю части (таблицы 0x49D12C и 0x476250, LUT 0x463050). Клиппинг по прямоугольнику 0x58E2F0/F4/F8/FC, stride поверхности 0x58E2EC, три ветки — обрезка слева/справа/без. Единственный caller — 0x425AA8 (отрисовка статического объекта), где им рисуются «светлые кадры»-оверлеи (окна, огонь) при _DAT_00849598 != 0, иначе берётся обычный блит 0x44083F.
- приоритет: Не переносить — чистый DirectDraw-рендер; полезен только как подтверждение формата RLE-строк (байт n>0 рисовать, n<0 пропуск, 0 конец), который в konung2/res.py уже есть.
- ключевые числа: Софтверный блендинг RGB565 через lookup-таблицы — в браузере это делает canvas. Сам порядок отрисовки объекта (VA 0x425AA8) и правило «светлые кадры не подменяют чужие пиксели» уже воспроизведены в knyaz2/web/static/entities.js и selfcheck.js, RLE-формат спрайтов разобран в konung2/res.py. Переносить нечего.
